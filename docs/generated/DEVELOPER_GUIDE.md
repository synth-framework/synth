# Developer Guide

> Generated by the Synth Documentation Expedition.

## Building on Synth

This guide is for contributors extending Synth.

## Developer Concepts

- **202 tests passing** — 202 tests passing.
- **40 capabilities mapped to unit tests** — 40 capabilities mapped to unit tests, integration tests, benchmarks, proofs, lint scope, and typecheck scope.
- **A capability can only be registered** — A capability can only be registered once (no duplicates)
- **A capability name** — A capability name
- **A capability needs governance constraints** — A capability needs governance constraints
- **A generic versioning capability contract exists** — A generic versioning capability contract exists and is documented.
- **A Git adapter implements the contract** — A Git adapter implements the contract and passes deterministic certification tests.
- **A GitHub forge adapter can create** — A GitHub forge adapter can create and merge pull requests through governed capability invocations.
- **A governed capability invocation can create** — A governed capability invocation can create and merge a pull request on GitHub.
- **A governed capability invocation can fork** — A governed capability invocation can fork a repository.
- **A mock adapter can drive the** — A mock adapter can drive the same state machine for tests.
- **A TDD adapter change reports TddAdapter** — A TDD adapter change reports `TddAdapter` and `AdapterRegistry` as affected.
- **A TDD adapter change runs adapter** — A TDD adapter change runs adapter tests and registry tests but skips replay.
- **A WebConsumer contract test verifies the** — A `WebConsumer` contract test verifies the consumer can produce a JSON payload suitable for HTTP APIs.
- **Accept Observation from Evidence Adapters** — Accept `Observation[]` from Evidence Adapters.
- **Accessibility tests** — 1.6 Accessibility tests
- **Adapter Adapter Registry** — Adapter / Adapter Registry
- **Adapter Architecture** — Adapter Architecture
- **Adapter architecture EXP-ADP-000 constitutionalizes the adapter** — Adapter architecture — EXP-ADP-000 constitutionalizes the adapter contract; the architecture is organized into three strata (Constitution, Kernel, Adapters); the Repository Adapter (Git reference) isolates all Git interaction behind the stable interface.
- **Adapter Architecture is constitutionalized EXP-ADP-000 defines** — **Adapter Architecture is constitutionalized**: EXP-ADP-000 defines the canonical adapter lifecycle, health model, and governance contract; the architecture is organized into three strata (Constitution, Kernel, Adapters).
- **Adapter capabilities are discoverable** — Adapter capabilities are discoverable.
- **Adapter Capability Model** — Adapter Capability Model
- **Adapter CLI** — Adapter CLI
- **Adapter CLI output separate surface** — Adapter CLI output (separate surface).
- **Adapter Contract** — 10. Adapter Contract
- **Adapter contract EXP-ADP-000** — Adapter contract (EXP-ADP-000)
- **Adapter determinism classification** — Adapter determinism classification
- **Adapter determinism is not systematically classified** — Adapter determinism is not systematically classified.
- **Adapter generates tests or documentation** — Adapter generates tests or documentation.
- **Adapter health is observable** — Adapter health is observable.
- **Adapter implementation** — Adapter implementation
- **Adapter implementations that conform to the** — Adapter implementations that conform to the existing adapter architecture.
- **Adapter is optional** — Adapter is optional.
- **Adapter is replaceable** — Adapter is replaceable.
- **Adapter is self-contained** — Adapter is self-contained.
- **Adapter Lifecycle** — Adapter Lifecycle
- **Adapter lifecycle unchanged** — Adapter lifecycle — unchanged.
- **Adapter metadata has been modified** — Adapter metadata has been modified.
- **Adapter observations can be collected through** — Adapter observations can be collected through `SynthAPI.adapterOperation({ operation: "observe" })`.
- **Adapter orchestration AdapterRegistry** — Adapter orchestration (`AdapterRegistry`)
- **Adapter owns all Git interaction** — Adapter owns all Git interaction.
- **Adapter passes lifecycle and health checks** — Adapter passes lifecycle and health checks.
- **Adapter redesign** — Adapter redesign.
- **Adapter registries and orchestrators** — 5. Adapter registries and orchestrators
- **Adapter Registry** — Adapter Registry
- **Adapter registry per drift class** — Adapter registry per drift class
- **Adapter Responsibilities** — Adapter Responsibilities
- **Adapter State** — Adapter State
- **Adapter Taxonomy** — Adapter Taxonomy
- **Adapter-specific metadata** — Adapter-specific metadata.
- **AdapterRegistry with 17 adapter factories** — `AdapterRegistry` with 17 adapter factories
- **Adapters** — Adapters
- **Adapters adapter- test js 17 files** — 1. Adapters (`adapter-*.test.js`) — 17 files
- **Adapters are first-class the Repository Adapter** — **Adapters are first-class**: the Repository Adapter (Git reference) isolates all Git interaction behind a stable interface.
- **Adapters are independently replaceable** — Adapters are independently replaceable.
- **Adapters are isolated from the kernel** — Adapters are isolated from the kernel.
- **Adapters are registered in src adapters** — Adapters are registered in `src/adapters/registry.ts`.
- **Adapters are surfaced as a first-class** — Adapters are surfaced as a first-class capability with a clear link to adapter documentation.
- **Adapters declare interaction capabilities** — Adapters declare interaction capabilities.
- **Adapters emit only canonical Observations** — Adapters emit only canonical Observations.
- **Adapters repository git github tdd bdd** — Adapters: repository/git, github, tdd, bdd, conversation, document, filesystem, specification, knowledge-extraction, confidence, dependency, architecture, mission-builder, expedition-builder, objective-builder, wizard
- **adapters with id version determinism** — `adapters` with id, version, determinism
- **Add a migration test a project** — Add a migration test: a project with legacy `data/` is opened, the CLI migrates it, and replay produces the same canonical state as before.
- **Add certification tests for runtime event** — Add certification tests for runtime event guarantees.
- **Add comprehensive tests for projection DAG** — Add comprehensive tests for projection DAG execution, rule composition, and validation.
- **Add contract and integration tests for** — Add contract and integration tests for both capabilities.
- **Add contract certification tests** — 6.8 Add contract certification tests
- **Add contract tests** — Add contract tests.
- **Add deterministic certification tests** — Add deterministic certification tests.
- **Add deterministic certification tests with a** — Add deterministic certification tests with a mock forge.
- **Add info subcommand to the adapter** — Add `info` subcommand to the adapter CLI.
- **Add MCP server capability to docs** — Add MCP server capability to `docs/reference/capability-validation-map.json`.
- **Add new source adapters deferred to** — Add new source adapters (deferred to EXP-DISCOVERY-003).
- **Add tests agent-sdk test js covering** — Add `tests/agent-sdk.test.js` covering protocol and metadata exports.
- **Add tests for store versioning diff** — Add tests for store, versioning, diff, and reconstruction.
- **Add tests for synth-framework agent-sdk exports** — Add tests for `@synth-framework/agent-sdk` exports.
- **Add tests mcp-server test js** — Add `tests/mcp-server.test.js`.
- **Add tests proving bootstrap uses Discovery** — Add tests proving bootstrap uses Discovery output.
- **Add the Brownfield Certification Test Suite** — Add the Brownfield Certification Test Suite.
- **Added a Capability Layers section to** — Added a **Capability Layers** section to `docs/reference/capability-reference.md` distinguishing domain capabilities (mutate canonical state, frozen registry) from environment capabilities (describe the environment, never mutate state).
- **Added CLI commands tdd-generate-test tdd-verify-failure tdd-verify-implementation** — Added CLI commands: `tdd-generate-test`, `tdd-verify-failure`, `tdd-verify-implementation`, `tdd-evidence`.
- **Added governance tests proving source-agnostic convergence** — Added governance tests proving source-agnostic convergence, no implementation assumptions, and no expedition/mission creation during initialization.
- **Added npm run test installer-contract and** — Added `npm run test:installer-contract` and included it in `npm run test:all`.
- **Added npm run test installer-distribution and** — Added `npm run test:installer-distribution` and included it in `npm run test:all`.
- **Added npm run test installer-environment and** — Added `npm run test:installer-environment` and included it in `npm run test:all`.
- **Added regression guards in tests operator-briefing** — Added regression guards in `tests/operator-briefing.test.js` covering:
- **Added regression tests in tests environment-capability-graph** — Added regression tests in `tests/environment-capability-graph.test.js` covering:
- **Added regression tests in tests environment-discovery** — Added regression tests in `tests/environment-discovery.test.js` covering:
- **Added regression tests in tests environment-filesystem-capability** — Added regression tests in `tests/environment-filesystem-capability.test.js` covering:
- **Added regression tests in tests environment-revision-capability** — Added regression tests in `tests/environment-revision-capability.test.js` covering:
- **Added regression tests in tests environment-workspace-capability** — Added regression tests in `tests/environment-workspace-capability.test.js` covering:
- **Added test environment-capability-graph npm script and** — Added `test:environment-capability-graph` npm script and included it in `test:all`.
- **Added test environment-discovery npm script and** — Added `test:environment-discovery` npm script and included it in `test:all`.
- **Added test environment-filesystem npm script and** — Added `test:environment-filesystem` npm script and included it in `test:all`.
- **Added test environment-revision npm script and** — Added `test:environment-revision` npm script and included it in `test:all`.
- **Added test environment-workspace npm script and** — Added `test:environment-workspace` npm script and included it in `test:all`.
- **Added test synth-cli npm script and** — Added `test:synth-cli` npm script and wired it into `test:all` and the CLI capability map.
- **Added tests adapter-introspection test js covering** — Added `tests/adapter-introspection.test.js` covering repository, filesystem, unknown adapter, and default-to-repository behavior.
- **Added tests clean-machine-output test js covering** — Added `tests/clean-machine-output.test.js` covering:
- **Added tests freeze-certification test js** — Added `tests/freeze-certification.test.js`.
- **Added tests github-adapter test js with** — Added `tests/github-adapter.test.js` with mocked GitHub API responses (9 tests, all passing).
- **Added tests installer-upgrade test js covering** — Added `tests/installer-upgrade.test.js` covering upgrade, exact version install, channel selection, and rollback on failure.
- **Added tests tdd-adapter test js with** — Added `tests/tdd-adapter.test.js` with 7 tests, all passing.
- **Adding a capability requires a system** — Adding a capability requires a system restart (bootstrap + seal)
- **Adding a New Capability** — Adding a New Capability
- **Adding a new capability does not** — Adding a new capability does not risk breaking existing ones
- **Adding new adapter capabilities** — Adding new adapter capabilities.
- **Adoption validation test tests adoption-validation test** — **Adoption validation test** — `tests/adoption-validation.test.js` verifies protocols, benchmark, report, and npm scripts.
- **ADR ADR-011 Process Tool Capability Accepted** — **ADR:** ADR-011 — Process & Tool Capability (Accepted).
- **ADR ADR-012 Runtime Package Capability Accepted** — **ADR:** ADR-012 — Runtime & Package Capability (Accepted).
- **ADR ADR-013 Forge Capability Accepted** — **ADR:** ADR-013 — Forge Capability (Accepted).
- **ADR ADR-014 Secrets Identity Capability Accepted** — **ADR:** ADR-014 — Secrets & Identity Capability (Accepted), including the constitutional **Non-Disclosure Rule**: secret values are never bulk-listed, logged, or included in evidence/replay/proof — only names are discoverable.
- **ADR-0005 Capability Registry** — ADR-0005: Capability Registry
- **ADR-0005 Capability Registry The registry is** — ADR-0005: Capability Registry — The registry is frozen by seal
- **ADR-007 Capability Graph Model** — ADR-007 — Capability Graph Model
- **ADR-008 Workspace Capability** — ADR-008 — Workspace Capability
- **ADR-009 Revision Capability** — ADR-009 — Revision Capability
- **ADR-010 Filesystem Capability** — ADR-010 — Filesystem Capability
- **ADR-011 Process Tool Capability** — ADR-011 — Process & Tool Capability
- **ADR-012 Runtime Package Capability** — ADR-012 — Runtime & Package Capability
- **ADR-013 Forge Capability** — ADR-013 — Forge Capability
- **ADR-014 Secrets Identity Capability** — ADR-014 — Secrets & Identity Capability
- **ADR-044 External Build Systems Are Adapters** — ADR-044 — External Build Systems Are Adapters
- **Agents execute test and deploy independently** — Agents execute, test, and deploy independently
- **AGENTS md new Environment capability planning** — **AGENTS.md:** new "Environment capability planning" section referencing the report and ADR-016.
- **AI benchmark testing demonstrates deterministic convergence** — AI benchmark testing demonstrates deterministic convergence across supported models. (Pending live model runs)
- **AI chat adapters present equivalent conversational** — AI chat adapters present equivalent conversational interactions.
- **All 152 tests passing synth 121** — All 152 tests passing (synth: 121, proposal-evaluation: 15, governance-evaluation: 7, convergence-certification: 9)
- **All existing documentation tests continue to** — All existing documentation tests continue to pass.
- **All existing tests continue to pass** — All existing tests continue to pass.
- **All existing tests must pass** — All existing tests must pass
- **All existing tests pass** — All existing tests pass.
- **All external interactions occur through capability** — All external interactions occur through capability providers.
- **All generated projections pass tests first-contact-projection** — All generated projections pass `tests/first-contact-projection.test.js` and regenerate byte-identically.
- **All installer tests and documentation integrity** — All installer tests and documentation integrity checks pass locally.
- **All new tests pass and npm** — All new tests pass and `npm run govern` succeeds in CI.
- **All tests pass and npm run** — All tests pass and `npm run govern` succeeds.
- **All tests updated** — All tests updated
- **All VersioningCapability operations are implemented for** — All `VersioningCapability` operations are implemented for Git.
- **Allows pluggable optimization adapters** — Allows pluggable optimization adapters.
- **an adapter-to-adapter translator** — an adapter-to-adapter translator,
- **An IdeConsumer contract test verifies the** — An `IdeConsumer` contract test verifies the consumer can produce language-server-friendly diagnostics from a session.
- **An McpConsumer contract test verifies the** — An `McpConsumer` contract test verifies the consumer can produce MCP resources/tools/prompts shapes.
- **An optional ArchitectureProjectionAdapter defaults to RuleBasedArchitectureProjectionAdapter** — An optional `ArchitectureProjectionAdapter`; defaults to `RuleBasedArchitectureProjectionAdapter`.
- **An optional CapabilityVerifier adapter defaults to** — An optional `CapabilityVerifier` adapter; defaults to `RuleBasedCapabilityVerifier`.
- **An optional DomainModelingAdapter defaults to RuleBasedDomainModelingAdapter** — An optional `DomainModelingAdapter`; defaults to `RuleBasedDomainModelingAdapter`.
- **An optional IntentModelingAdapter defaults to RuleBasedIntentModelingAdapter** — An optional `IntentModelingAdapter`; defaults to `RuleBasedIntentModelingAdapter`.
- **An optional KnowledgeModelingAdapter defaults to RuleBasedKnowledgeAdapter** — An optional `KnowledgeModelingAdapter`; defaults to `RuleBasedKnowledgeAdapter`.
- **An optional ValidationAdapter defaults to RuleBasedValidationAdapter** — An optional `ValidationAdapter`; defaults to `RuleBasedValidationAdapter`.
- **API api-adapter-integration** — API (`api-adapter-integration`)
- **API response includes attestation** — API response includes attestation
- **API translation layer handles legacy capability** — API translation layer handles legacy capability names
- **Architecture Capability Model Execution artifact capabilities** — Architecture: Capability Model — Execution artifact capabilities
- **Article IX Capability Control** — Article IX — Capability Control
- **Article IX Capability Control Provisions 27-29** — Article IX — Capability Control (Provisions 27-29)
- **AsyncAPI files produce evidence and capability** — AsyncAPI files produce evidence and capability observations.
- **Attestation** — Attestation
- **AUDIT-2026-06-29-C4 Capability and Policy Registries Are** — AUDIT-2026-06-29-C4: Capability and Policy Registries Are Not Frozen in Modular Source
- **AUDIT-2026-06-29-O2 202 Tests Pass but Verify** — AUDIT-2026-06-29-O2: 202 Tests Pass but Verify Stubs
- **Author commits through VersioningCapability** — Author commits through VersioningCapability.
- **Automated tests require an accessibility testing** — Automated tests require an accessibility testing library.
- **Below-the-fold capability grid architecture explorer and** — Below-the-fold capability grid, architecture explorer, and documentation links load only when scrolled into view.
- **Bind permit to specific invocation capability** — Bind permit to specific invocation (capability, actor)
- **Bootstrap contract is deterministic and testable** — Bootstrap contract is deterministic and testable.
- **Bootstrap contract is published and testable** — Bootstrap contract is published and testable.
- **Bootstrap environment capability report** — Bootstrap (environment capability report)
- **Both included in every attestation** — Both included in every attestation
- **Branch creation and checkout through VersioningCapability** — Branch creation and checkout through VersioningCapability.
- **Bridge 1 Adapter Observation Mapper** — Bridge 1: Adapter Observation Mapper
- **Bridge 2 Adapter Observation Collector** — Bridge 2: Adapter Observation Collector
- **Brownfield Certification Test Suite** — 6. Brownfield Certification Test Suite
- **Build a capability graph from capability** — Build a capability graph from `capability` observations.
- **Build and full test suite remain** — Build and full test suite remain green.
- **Build test npm run build and** — **Build/test:** `npm run build` and `npm test` pass (121 passed, 0 failed).
- **buildCapabilityReport createCapabilityGraph src discovery projections** — `buildCapabilityReport` / `createCapabilityGraph` — `src/discovery/projections/`
- **C GitHub adapter naming collision KEEP** — C. GitHub adapter naming collision — KEEP SEPARATE, RENAME
- **Candidate 1 builder adapters into planning** — **Candidate 1** (builder adapters into planning/mission-studio)
- **Candidate 2 environment vs adapters** — **Candidate 2** (environment vs. adapters)
- **Cannot directly invoke another capability** — Cannot directly invoke another capability
- **Canonical state adapter both model repository** — **Canonical state + adapter both model repository state.**
- **Capabilities can be understood and tested** — Capabilities can be understood and tested independently
- **Capability** — 1.4 Capability
- **Capability additions** — Capability additions.
- **Capability Areas** — Capability Areas
- **Capability as Execution Projection** — Capability as Execution Projection
- **capability capability name** — `capability`: capability name
- **Capability Capability Registry** — Capability / Capability Registry
- **Capability cards use the entire card** — Capability cards use the entire card as a hit area with a visible focus ring.
- **Capability Compatibility** — 9.3 Capability Compatibility
- **Capability Composition** — 3. Capability Composition
- **Capability Dependencies** — Capability Dependencies
- **Capability details** — Capability details
- **Capability Discovery** — Capability Discovery
- **Capability dispatch route each intent to** — Capability dispatch: route each intent to the appropriate registered capability.
- **Capability documented as execution projection only** — Capability documented as execution projection only
- **capability e g listUsers UserService GetUser** — `capability` — e.g. "listUsers", "UserService.GetUser"
- **Capability Execution** — Capability Execution
- **Capability execution which already produces effects** — **Capability execution** (which already produces effects).
- **capability existing or needed capability** — `capability` — existing or needed capability
- **Capability Extension** — Capability Extension
- **Capability extension while preserving the Constitutional** — Capability extension while preserving the Constitutional Baseline. New kernel-level changes require ADRs and updated proofs.
- **Capability extensions** — Capability extensions
- **Capability failures block materialization with a** — Capability failures block materialization with a clear explanation.
- **Capability families Environment Workspace Filesystem Revision** — Capability families: Environment, Workspace, Filesystem, Revision, Process, Tool, Runtime, Package, Network, Forge, Secrets, Identity, Versioning
- **Capability Graph** — Capability Graph
- **Capability implementation** — Capability implementation
- **Capability implementations** — Capability implementations
- **Capability inventory** — Capability inventory
- **Capability is present and registered** — Capability is present and registered
- **Capability Isolation** — Capability Isolation
- **Capability Layers** — Capability Layers
- **Capability lookup** — Capability lookup
- **Capability map extended with missing capabilities** — Capability map extended with missing capabilities detected by the impact analyzer (`Scripts`, `GitHubActions`, `GitHooks`, `RepositoryConfig`, `Constitution`, `ArchitectureDecisionRecords`) and the new `ValidationPlanner` capability.
- **Capability Model** — Capability Model
- **Capability Model capability model and Capability** — 07 - Capability Model — capability model and Capability Graph
- **Capability Model Impact** — 5. Capability Model Impact
- **Capability Model semantics only additive family** — Capability Model semantics (only additive family extension)
- **Capability Model the model itself new** — Capability Model (the model itself; new providers may be registered)
- **capability name of the capability that** — `capability`: name of the capability that produced this event
- **Capability Names** — Capability Names
- **Capability names CreateTicket StartTicket** — Capability names (`CreateTicket`, `StartTicket`)
- **Capability names in registry alias entries** — **Capability names** in registry — alias entries
- **Capability Naming Conventions** — Capability Naming Conventions
- **Capability Node Schema** — 2. Capability Node Schema
- **Capability nodes one per canonical family** — **Capability nodes** — one per canonical family (`cap:Filesystem`, `cap:Revision`, ...). Each records its family, schema version, whether it is required for execution, and human-readable metadata.
- **Capability observations produce a capability graph** — Capability observations produce a capability graph.
- **Capability observations produce a mission when** — Capability observations produce a mission when no higher-level source exists.
- **Capability Permissions** — Capability Permissions
- **Capability Reference** — Capability Reference
- **Capability Reference domain and environment capability** — Capability Reference — domain and environment capability families
- **Capability registration** — Capability registration
- **Capability registrations** — Capability registrations
- **Capability Registry** — Capability Registry
- **Capability registry freeze** — Capability registry freeze
- **Capability registry freeze unchanged No new** — Capability registry freeze — unchanged. No new capabilities registered post-seal.
- **Capability Registry lines 941-966** — Capability Registry (lines 941-966)
- **Capability registry src capability registry ts** — **Capability registry:** `src/capability/registry.ts`
- **Capability Report Format** — 1. Capability Report Format
- **Capability Resolution** — 6.1 Capability Resolution
- **capability routing** — capability routing
- **Capability Semantics** — 6. Capability Semantics
- **Capability sequences** — Capability sequences
- **capability the capability family it informs** — `capability` — the capability family it informs.
- **Capability Verification** — 3.1 Capability Verification
- **Capability verification confirms feasibility before Mission** — Capability verification confirms feasibility before Mission creation.
- **Capability verification confirms that the selected** — Capability verification confirms that the selected architecture can be realized.
- **Capability verification EXP-AIFC-006** — Capability verification (EXP-AIFC-006).
- **Capability verification EXP-GENESIS-003** — Capability verification (EXP-GENESIS-003).
- **Capability verification framework EXP-GENESIS-003** — Capability verification framework (EXP-GENESIS-003).
- **Capability verification gates materialization Missing or** — **Capability verification gates materialization.** Missing or degraded required capabilities block Mission creation.
- **Capability verification produces deterministic feasibility evidence** — Capability verification produces deterministic feasibility evidence.
- **Capability verification report** — Capability verification report.
- **Capability verification results are attached as** — Capability verification results are attached as evidence to the Mission.
- **capability versions or adapter metadata when** — capability versions or adapter metadata when relevant.
- **capability what capability was used** — `capability` — what capability was used
- **Capability-Based** — Capability-Based
- **Capability-based security models** — Capability-based security models
- **Capability-driven** — Capability-driven
- **Capability-driven the Kernel invokes capabilities never** — **Capability-driven** — the Kernel invokes capabilities, never adapter-specific functions.
- **capability-registry extractor** — capability-registry extractor
- **CapabilityCard individual capability card** — `CapabilityCard` — individual capability card.
- **CapabilityIcon semantic icon per capability** — `CapabilityIcon` — semantic icon per capability.
- **CapabilityRegistry** — CapabilityRegistry
- **capabilityRegistry intentRegistry** — `capabilityRegistry` → `intentRegistry`
- **Certification report generated at data-test operator-journey-certification** — Certification report generated at `data-test/operator-journey-certification.json`.
- **Certification tests pass and use only** — Certification tests pass and use only public CLI commands and documented artifacts.
- **Certification tests proving determinism and read-only** — Certification tests proving determinism and read-only behavior.
- **Certify agent compliance through deterministic test** — Certify agent compliance through deterministic test suites.
- **Changes to the capability model or** — Changes to the capability model or constitutional baseline.
- **Changes to the kernel EventStore StateStore** — Changes to the kernel, EventStore, StateStore, Replay, or ExecutionGate.
- **Changing adapter interfaces or implementations** — Changing adapter interfaces or implementations.
- **Changing the environment capability model** — Changing the environment capability model.
- **ChatGPT Gemini Claude and Cursor adapter** — ChatGPT, Gemini, Claude, and Cursor adapter charters
- **Check battery green first-contact tests 6** — Check battery green: first-contact tests 6/6, documentation integrity tests pass, `docs:check-links` (847 internal links resolve), `docs:verify-projection`, `docs:verify-website-sync`.
- **CI adapters invoke npm run govern** — CI adapters invoke `npm run govern` only.
- **CI wiring test taskpro-regression added to** — **CI wiring** — `test:taskpro-regression` added to `package.json` and `test:all`.
- **Classification Test infrastructure is Application simplifiable** — Classification: Test infrastructure is **Application** (simplifiable).
- **Clean install test package installs from** — Clean install test: package installs from packed tarball in an isolated directory.
- **CLI adapters present equivalent terminal interactions** — CLI adapters present equivalent terminal interactions.
- **CLI synth doctor capability reporting** — CLI `synth doctor` / capability reporting
- **Code generation quality beyond capability verification** — Code generation quality beyond capability verification.
- **Collision A CLI Test Harness highest** — Collision A — CLI Test Harness (highest impact)
- **Collision D Adapter Lifecycle Assertions** — Collision D — Adapter Lifecycle Assertions
- **Collision H Environment Capability Contexts** — Collision H — Environment Capability Contexts
- **Collision L Adapter Document Filesystem Specification** — Collision L — Adapter Document/Filesystem/Specification Fixture Cleanup
- **command executable command or capability invocation** — `command` — executable command or capability invocation
- **Commands adapter agent-artifacts ai-interaction-manifest ai-metadata bootstrap-analyzer** — Commands: `adapter`, `agent-artifacts`, `ai-interaction-manifest`, `ai-metadata`, `bootstrap-analyzer`, `bootstrap-apply`, `bootstrap-context`, `certification-runner`, `command-safety`, `explain-governance`, `explain-observability`, `first-contact`, `govern-delegation`, `repo`, `repository-identity`, `resume-briefing`, `status-briefing`, `synth`, `verify`.
- **Comparison Ticket WorkItem vs Capability Intent** — Comparison: Ticket → WorkItem vs Capability → Intent
- **Compatibility suite for legacy capability field** — Compatibility suite for legacy `capability` field
- **Compatibility Tests** — Compatibility Tests
- **Component tests website js components test** — Component tests: `website/js/components.test.js`
- **Comprehensive test suite** — Comprehensive test suite
- **computeStateHash excludes derived fields so state** — `computeStateHash` excludes derived fields, so state hashes will change. This is expected and will be captured in migration evidence.
- **confidence coverage of the capability map** — `confidence`: coverage of the capability map.
- **Configures the filesystem observation capability with** — Configures the filesystem observation capability with the target directory.
- **Confirm permit matches the invocation capability** — Confirm permit matches the invocation (capability, actor)
- **Confusing test failures that are artifacts** — Confusing test failures that are artifacts of the agent's own execution, not the code under change.
- **Connect to CapabilityRegistry** — Connect to CapabilityRegistry
- **Contract tests asserting all IDE rules** — Contract tests asserting all IDE rules are deterministic and fresh.
- **Contract tests compile and pass npm** — Contract tests compile and pass; `npm run build` succeeds.
- **Contract tests proving that every agent** — Contract tests proving that every agent skill is derived from the canonical model and remains deterministic.
- **Contract tests proving that identical model** — Contract tests proving that identical model inputs produce deterministic outputs.
- **Contract tests verifying handshake tool advertisement** — Contract tests verifying handshake, tool advertisement, and read-only execution.
- **Convention-based metadata in test and script** — Convention-based metadata in test and script files
- **conversation adapter overlaps with First Contact** — `conversation` adapter overlaps with **First Contact** intent capture.
- **ConversationAdapter with natural-language intent** — `ConversationAdapter` with natural-language intent
- **Core depends only on adapter capabilities** — Core depends only on adapter capabilities.
- **createCapabilityRegistry createIntentRegistry** — `createCapabilityRegistry()` → `createIntentRegistry()`
- **Created tests installer-engine test js covering** — Created `tests/installer-engine.test.js` covering dry-run, success, retry, cleanup, idempotency, and upgrade paths.
- **Created tests installer-verify test js covering** — Created `tests/installer-verify.test.js` covering missing synth, successful verify-only, version mismatch, proof emission, and unhealthy doctor scenarios.
- **createInfra selecting NodeFilesystemAdapter versus InMemoryFilesystemAdapter based** — `createInfra()` selecting `NodeFilesystemAdapter` versus `InMemoryFilesystemAdapter` based on a configuration flag.
- **Cross-capability coordination requires explicit event-based communication** — Cross-capability coordination requires explicit event-based communication
- **Cross-run equivalence is tested incidentally not** — Cross-run equivalence is tested incidentally, not certified.
- **Cryptographically Attested** — Cryptographically Attested
- **Current capability boundary** — 4. Current capability boundary
- **Current cost lines of code test** — **Current cost** — lines of code, test surface, conceptual load.
- **custom adapter-specific category** — `custom` — adapter-specific category
- **D Rule-based adapter pattern EXTRACT SHARED** — D. Rule-based adapter pattern — EXTRACT SHARED UTILITY, KEEP ADAPTERS
- **D1 Capability location** — D1 — Capability location
- **data-test operator-journey-certification json generated certification report** — `data-test/operator-journey-certification.json` — generated certification report with evidence.
- **data-test test fixture and scratch directory** — `data-test/` — test fixture and scratch directory. It is not runtime state and stays at the repository root.
- **Decision accept adjust-and-retest or reject** — Decision: accept, adjust-and-retest, or reject
- **Dedicated test scripts in test all** — Dedicated test scripts in `test:all`: `test:adapter`, `test:github`, `test:tdd`, `test:bdd`, `test:conversation`, `test:document`, `test:filesystem`, `test:specification`, `test:knowledge-extraction`, `test:confidence`, `test:dependency`, `test:architecture`, `test:mission-builder`, `test:expedition-builder`, `test:objective-builder`, `test:wizard`.
- **Default channel is latest** — Default channel is `latest`.
- **Define a generic ProjectionCapability TOutput contract** — Define a generic `ProjectionCapability<TOutput>` contract.
- **Define Capability Families** — 2. Define Capability Families
- **Define event emission for each capability** — Define event emission for each capability
- **Define the ObservationCapability contract** — Define the `ObservationCapability` contract.
- **Defined InitializationAdapter contract in src adapters** — Defined `InitializationAdapter` contract in `src/adapters/initialization-adapter.ts`.
- **Delete empty obsolete environment capability interface** — Delete empty/obsolete environment capability interface files.
- **Delete src adapters registry ts if** — Delete `src/adapters/registry.ts` if not already removed.
- **Deleted data-test ephemeral test artifacts** — Deleted `data-test/` (ephemeral test artifacts).
- **Depends on EXP-COMPLEXITY-AUDIT-001 EXP-CAPABILITY-BOUNDARY-001** — Depends on: `EXP-COMPLEXITY-AUDIT-001`, `EXP-CAPABILITY-BOUNDARY-001`
- **Depends on EXP-COMPLEXITY-AUDIT-001 EXP-SIMPLIFICATION-002 test infrastructure** — Depends on: `EXP-COMPLEXITY-AUDIT-001`, `EXP-SIMPLIFICATION-002` (test infrastructure)
- **Derive capability observations from specification capability** — Derive `capability` observations from specification capability and dependency observations.
- **Detect build test and configuration files** — Detect build, test, and configuration files.
- **Detect implementation test documentation architecture and** — Detect implementation, test, documentation, architecture, and knowledge directories.
- **Detect missing tests** — Detect missing tests
- **Detect test files and test scripts** — Detect test files and test scripts.
- **Determinism Test** — Determinism Test
- **Deterministic Adapter Classification** — Deterministic Adapter Classification
- **Deterministic adapters are certified for cross-run** — Deterministic adapters are certified for cross-run equivalence.
- **Deterministic certification tests for each versioning** — Deterministic certification tests for each versioning operation using real Git repositories.
- **Deterministic lifecycle transitions and capability behavior** — **Deterministic** — lifecycle transitions and capability behavior are reproducible.
- **Deterministic rule-based adapters** — 1.2 Deterministic rule-based adapters
- **Discovery discovery- test js 15 files** — 2. Discovery (`discovery-*.test.js`) — 15 files
- **Discovery link Audit requirements team capability** — Discovery link: Audit requirements, team capability assessment
- **Discovery link Load testing team structure** — Discovery link: Load testing, team structure analysis
- **DiscoveryAdapterRegistry** — `DiscoveryAdapterRegistry`
- **DiscoveryRule CapabilityProvider** — `DiscoveryRule` + `CapabilityProvider`
- **Do not delete or rewrite test** — Do not delete or rewrite test scenarios.
- **Do not implement the capability** — Do not implement the capability.
- **Do not migrate all tests to** — Do not migrate all tests to shared helpers unless those tests are already being touched.
- **Do not migrate tests to SDK** — Do not migrate tests to SDK unless those tests are already being touched.
- **Do not redesign adapter discovery or** — Do not redesign adapter, discovery, or environment interfaces.
- **Do not redesign EventStore StateStore Replay** — Do not redesign EventStore, StateStore, Replay, or ExecutionGate.
- **Do not unify FilesystemAdapter and FilesystemProvider** — Do not unify `FilesystemAdapter` and `FilesystemProvider` unless that unification is explicitly scoped and safe.
- **docs ADAPTER ARCHITECTURE md docs adapter-architecture** — `docs/ADAPTER_ARCHITECTURE.md` → `docs/adapter-architecture.md`
- **docs adr ADR-007-capability-graph-model md** — `docs/adr/ADR-007-capability-graph-model.md`
- **docs adr ADR-008-workspace-capability md** — `docs/adr/ADR-008-workspace-capability.md`
- **docs adr ADR-010-filesystem-capability md** — `docs/adr/ADR-010-filesystem-capability.md`
- **docs adr ADR-011-process-tool-capability md** — `docs/adr/ADR-011-process-tool-capability.md`
- **docs adr ADR-012-runtime-package-capability md** — `docs/adr/ADR-012-runtime-package-capability.md`
- **docs architecture 07-capability-model md accurately describes** — `docs/architecture/07-capability-model.md` accurately describes the Capability Graph and the Environment Layer boundary.
- **docs design proposal-evaluation-capability md this document** — `docs/design/proposal-evaluation-capability.md` — this document
- **docs expeditions EXP-CAPABILITY-BOUNDARY-001 md** — `docs/expeditions/EXP-CAPABILITY-BOUNDARY-001.md`
- **docs reference capability-validation-map json remains the** — **docs/reference/capability-validation-map.json** remains the canonical capability-to-test mapping consumed by the Validation Planner.
- **Documentation integrity and projection tests pass** — Documentation integrity and projection tests pass.
- **Documentation integrity checks npm run test** — Documentation integrity checks (`npm run test:documentation-integrity`) pass after fixing stale relative links in `examples/first-contact/docs-generated/`.
- **Documentation projection tests pass** — Documentation projection tests pass.
- **Documented all twelve environment capability families** — Documented all twelve environment capability families in `docs/reference/capability-reference.md`: description, required flag, provider contract, default provider(s), and governing ADR — derived from `src/environment/types.ts`, `src/environment/graph.ts`, and the capability contract files.
- **Domain Tests 8 tests** — Domain Tests (8 tests)
- **Drafted and approved ADR-007 Capability Graph** — Drafted and approved **ADR-007 — Capability Graph Model**.
- **Drafted and approved ADR-008 Workspace Capability** — Drafted and approved **ADR-008 — Workspace Capability**.
- **Drafted and approved ADR-009 Revision Capability** — Drafted and approved **ADR-009 — Revision Capability**.
- **Drafted and approved ADR-010 Filesystem Capability** — Drafted and approved **ADR-010 — Filesystem Capability**.
- **Duplicate capability cases in applyDomain** — Duplicate capability cases in `applyDomain`
- **Duplicate capability entries in the registry** — Duplicate capability entries in the registry
- **Each adapter has a dedicated adapter** — Each adapter has a dedicated `adapter.ts` and `types.ts`, following a uniform pattern.
- **Each capability is an Artifact Card** — Each capability is an Artifact Card variant.
- **Each capability maps to a runtime** — Each capability maps to a runtime SYNTH concept.
- **Each layer can be tested independently** — Each layer can be tested independently
- **Each referenced capability in execution claims** — Each referenced capability in execution claims actually exists in `execution.ts`.
- **Each skill has a README tests** — Each skill has a README, tests, and a usage example.
- **Each step is testable** — Each step is testable.
- **Easier Forge metadata becomes capability evidence** — **Easier:** Forge metadata becomes capability evidence (supports EXP-ENV-010).
- **Easier Provider selection becomes explicit testable** — **Easier:** Provider selection becomes explicit, testable, and inspectable.
- **Easier Runtime detection becomes capability evidence** — **Easier:** Runtime detection becomes capability evidence (supports EXP-ENV-010).
- **Easier Secret availability becomes capability evidence** — **Easier:** Secret *availability* becomes capability evidence without disclosing values (supports EXP-ENV-010).
- **Easier Testing can use in-memory revision** — **Easier:** Testing can use in-memory revision providers.
- **Easier Tests can use in-memory filesystem** — **Easier:** Tests can use in-memory filesystem providers.
- **Easier Tests can use in-memory workspace** — **Easier:** Tests can use in-memory workspace providers.
- **Easier Tests can use scripted forge** — **Easier:** Tests can use scripted forge providers.
- **Easier Tests can use scripted or** — **Easier:** Tests can use scripted or in-memory process providers.
- **Easier Tests inject in-memory secret maps** — **Easier:** Tests inject in-memory secret maps — no real environment access.
- **Easier Tool discovery becomes capability evidence** — **Easier:** Tool discovery becomes capability evidence (supports EXP-ENV-010).
- **Easy to regression-test with in-memory filesystem** — Easy to regression-test with in-memory filesystem.
- **Edge Case Tests 6 tests** — Edge Case Tests (6 tests)
- **Emit capability Observations for declared operations** — Emit `capability` Observations for declared operations, channels, services, and RPCs.
- **End-to-end tests** — 1.4 End-to-end tests
- **End-to-end tests pass for the complete** — End-to-end tests pass for the complete guided experience.
- **Enforcement tests added in tests protected-asset-escalation** — Enforcement tests added in `tests/protected-asset-escalation.test.js` covering every Protected Asset path, non-protected changes, mixed changes, and multiple asset naming.
- **Environment Capability and Projection are architectural** — **"Environment"**, **"Capability"**, and **"Projection"** are architectural concepts that do not appear. This may be intentional for Era II public vocabulary simplicity, but it means visitors cannot discover these ideas from the website.
- **Environment capability discovery src environment** — **Environment capability discovery** (`src/environment/`).
- **Environment Capability Families** — Environment Capability Families
- **Environment capability report** — Environment capability report.
- **Environment certification tests** — Environment certification tests
- **Environment Providers environment- test js 12** — 3. Environment / Providers (`environment-*.test.js`) — 12 files
- **Estimated Test Count** — Estimated Test Count
- **Event Store Tests 3 tests** — Event Store Tests (3 tests)
- **EventStore StateStore CheckpointStore** — EventStore / StateStore / CheckpointStore
- **Every adapter capability maps to at** — Every adapter capability maps to at least one test.
- **Every adapter conforms to the canonical** — Every adapter conforms to the canonical lifecycle.
- **Every capability is validated governed and** — Every capability is validated, governed, and logged
- **Every environment capability family delivered by** — Every environment capability family delivered by EXP-PROGRAM-007 appears in the public capability reference.
- **Every evidence-producing adapter emits Observation** — Every evidence-producing adapter emits `Observation[]`.
- **Every implemented capability can be traced** — Every implemented capability can be traced back to an executable behavior specification.
- **Every registered adapter can be introspected** — Every registered adapter can be introspected.
- **Evidence file paths test counts production** — **Evidence** — file paths, test counts, production usage.
- **Evidence follows product quality The fastest** — **Evidence follows product quality.** The fastest way to grow is to make SYNTH work flawlessly.
- **Example CI adapter** — Example CI adapter
- **Examples are exercised by automated tests** — Examples are exercised by automated tests.
- **examples crm Business objects and adapter** — `examples/crm/` — Business objects and adapter integration patterns.
- **Execute capabilities through registered adapters** — Execute capabilities through registered adapters.
- **execute invocation CapabilityInvocation operational path** — `execute(invocation: CapabilityInvocation)` — operational path.
- **Execution execution- test js 5 files** — 9. Execution (`execution-*.test.js`) — 5 files
- **ExecutionArtifactAdapter New** — 6. ExecutionArtifactAdapter (New)
- **ExecutionCoordinator P1 Attestation** — ExecutionCoordinator (P1 Attestation)
- **Exercise capability failure and recovery** — Exercise capability failure and recovery.
- **Existing adapters can optionally implement observe** — Existing adapters can optionally implement `observe()` without breaking changes.
- **Existing muscle memory npm run test** — Existing muscle memory (`npm run test:foo`) must adapt or be shimmed.
- **Existing Repository Adapter tests continue to** — Existing Repository Adapter tests continue to pass.
- **Existing source code and tests remain** — Existing source code and tests remain intact.
- **EXP-ADP-000 Adapter Architecture Specification** — EXP-ADP-000 — Adapter Architecture Specification
- **EXP-ADP-001 Repository Adapter Git Reference Implementation** — EXP-ADP-001 — Repository Adapter (Git Reference Implementation)
- **EXP-ADP-002 GitHub Adapter** — EXP-ADP-002 — GitHub Adapter
- **EXP-ADP-003 TDD Adapter** — EXP-ADP-003 — TDD Adapter
- **EXP-ADP-004 BDD Adapter** — EXP-ADP-004 — BDD Adapter
- **EXP-ADP-005 Conversation Adapter** — EXP-ADP-005 — Conversation Adapter
- **EXP-ADP-006 Document Adapter** — EXP-ADP-006 — Document Adapter
- **EXP-ADP-007 Repository Adapter Observation Extension** — EXP-ADP-007 — Repository Adapter Observation Extension
- **EXP-ADP-008 Filesystem Adapter** — EXP-ADP-008 — Filesystem Adapter
- **EXP-ADP-009 Specification Adapter** — EXP-ADP-009 — Specification Adapter
- **EXP-ADP-010 Knowledge Extraction Adapter** — EXP-ADP-010 — Knowledge Extraction Adapter
- **EXP-ADP-011 Confidence Adapter** — EXP-ADP-011 — Confidence Adapter
- **EXP-ADP-012 Dependency Adapter** — EXP-ADP-012 — Dependency Adapter
- **EXP-ADP-013 Architecture Adapter** — EXP-ADP-013 — Architecture Adapter
- **EXP-ADP-014 Mission Builder Adapter** — EXP-ADP-014 — Mission Builder Adapter
- **EXP-ADP-015 Expedition Builder Adapter** — EXP-ADP-015 — Expedition Builder Adapter
- **EXP-ADP-016 Objective Builder Adapter** — EXP-ADP-016 — Objective Builder Adapter
- **EXP-ADP-017 Wizard Adapter** — EXP-ADP-017 — Wizard Adapter
- **EXP-ADP-OBS-001 Observation Adapter** — EXP-ADP-OBS-001 — Observation Adapter
- **EXP-AIFC-006 Capability Verification Framework** — EXP-AIFC-006 — Capability Verification Framework
- **EXP-CAPABILITY-BOUNDARY-001 Completion Evidence** — EXP-CAPABILITY-BOUNDARY-001 — Completion Evidence
- **EXP-CAPABILITY-BOUNDARY-001 created the MutationRequest boundary but** — `EXP-CAPABILITY-BOUNDARY-001` created the `MutationRequest` boundary, but adoption is incomplete.
- **EXP-CAPABILITY-BOUNDARY-001 Pre-Change Evidence** — EXP-CAPABILITY-BOUNDARY-001 — Pre-Change Evidence
- **EXP-CAPABILITY-BOUNDARY-001 Single Mutation Execution Boundary** — EXP-CAPABILITY-BOUNDARY-001 — Single Mutation Execution Boundary
- **EXP-DISC-003 Adapter Introspection** — EXP-DISC-003 — Adapter Introspection
- **EXP-DISC-003 Adapter introspection synth adapter info** — EXP-DISC-003 — Adapter introspection (`synth adapter info <name>`). *(N5)*
- **EXP-DISC-003 synth adapter info name exposes** — **EXP-DISC-003** — `synth adapter info <name>` exposes adapter metadata, state, and health.
- **EXP-DISCOVERY-001 Source Adapter Framework** — EXP-DISCOVERY-001 — Source Adapter Framework
- **EXP-DISCOVERY-004 Projection Capability Mechanism** — EXP-DISCOVERY-004 — Projection Capability Mechanism
- **EXP-DIST-001 Canonical AI Capability Model** — EXP-DIST-001 — Canonical AI Capability Model
- **EXP-DOCS-002 Capability Model Documentation** — EXP-DOCS-002 — Capability Model Documentation
- **EXP-DOCS-002 documented all twelve environment capability** — EXP-DOCS-002 documented all twelve environment capability families and the Capability Graph in the capability reference and architecture pages (PR #69).
- **EXP-ENV-002 Capability Graph Model** — EXP-ENV-002 — Capability Graph Model
- **EXP-ENV-003 Workspace Capability** — EXP-ENV-003 — Workspace Capability
- **EXP-ENV-004 Revision Capability** — EXP-ENV-004 — Revision Capability
- **EXP-ENV-005 Filesystem Capability** — EXP-ENV-005 — Filesystem Capability
- **EXP-ENV-006 Process Tool Capability** — EXP-ENV-006 — Process & Tool Capability
- **EXP-ENV-007 Runtime Package Capability** — EXP-ENV-007 — Runtime & Package Capability
- **EXP-ENV-008 Forge Capability** — EXP-ENV-008 — Forge Capability
- **EXP-ENV-009 Secrets Identity Capability** — EXP-ENV-009 — Secrets & Identity Capability
- **EXP-EXEC-005 added pull-request projection through ForgeCapability** — **EXP-EXEC-005** added pull-request projection through `ForgeCapability`, recording the PR locator in `EXPEDITION_EXECUTION_PROJECTED`.
- **EXP-GOVERNABILITY-001 This expedition designs the capability** — **EXP-GOVERNABILITY-001:** This expedition designs the capability that 001 identified as missing.
- **EXP-GOVERNABILITY-002 Proposal Evaluation Capability Design** — EXP-GOVERNABILITY-002 — Proposal Evaluation Capability Design
- **EXP-GOVERNABILITY-003 Proposal Evaluation Capability Implementation** — EXP-GOVERNABILITY-003 — Proposal Evaluation Capability Implementation
- **EXP-GOVERNABILITY-003 Proposal Evaluation Capability supplies the** — **EXP-GOVERNABILITY-003 — Proposal Evaluation Capability** supplies the deterministic evaluation layer consumed by Review and Acceptance Gates.
- **EXP-HOME-023 AI Operator Adapter** — EXP-HOME-023 — AI Operator Adapter
- **EXP-INIT-001 Adapter-based Project Bootstrap** — EXP-INIT-001 — Adapter-based Project Bootstrap
- **EXP-PROGRAM-015 Repository Versioning Capability** — EXP-PROGRAM-015 — Repository Versioning Capability
- **EXP-PROGRAM-027 Remains paused until the capability** — **EXP-PROGRAM-027:** Remains paused until the capability is implemented and the replay is re-executed.
- **EXP-PROGRAM-035 Will consume this capability for** — **EXP-PROGRAM-035:** Will consume this capability for Review Gate / Acceptance Gate enforcement.
- **EXP-PROGRAM-036 Corrective action program this capability** — **EXP-PROGRAM-036:** Corrective action program; this capability is part of its scope.
- **EXP-PROGRAM-038 Audit Remediation builds on EXP-CAPABILITY-BOUNDARY-001** — **EXP-PROGRAM-038 — Audit Remediation** builds on `EXP-CAPABILITY-BOUNDARY-001` to close execution-gate bypasses.
- **EXP-REFINE-014 Mission Projection Capability** — EXP-REFINE-014 — Mission Projection Capability
- **EXP-REPO-005 Forge Adapter Contract** — EXP-REPO-005 — Forge Adapter Contract
- **EXP-SIMPLIFICATION-002 Test Infrastructure Unification** — EXP-SIMPLIFICATION-002 — Test Infrastructure Unification
- **EXP-VAL-010 Capability Test Mapping** — EXP-VAL-010 — Capability ↔ Test Mapping
- **EXP-VCS-001 established the generic VersioningCapability contract** — **EXP-VCS-001** established the generic `VersioningCapability` contract.
- **EXP-VCS-001 Versioning Capability Contract** — EXP-VCS-001 — Versioning Capability Contract
- **EXP-VCS-002 delivered the Git reference adapter** — **EXP-VCS-002** delivered the Git reference adapter (`src/environment/git-versioning-provider.ts`).
- **EXP-VCS-002 Git Versioning Adapter** — EXP-VCS-002 — Git Versioning Adapter
- **EXP-VCS-003 GitHub Forge Adapter** — EXP-VCS-003 — GitHub Forge Adapter
- **EXP-VCS-005 added deterministic certification tests tests** — **EXP-VCS-005** added deterministic certification tests (`tests/environment-versioning-certification.test.js`) proving repeatable repository state across isolated runs.
- **Expand capability model beyond mutation boundary** — ❌ Expand capability model beyond mutation boundary
- **Expedition acceptance tests** — Expedition acceptance tests.
- **Explicit Governance Cryptographic Attestation Authorization is** — **Explicit Governance** + **Cryptographic Attestation** = Authorization is verifiable, not merely claimed
- **Exposes only eventStore guarded and stateStore** — Exposes only `eventStore` (guarded) and `stateStore`.
- **Extend the Forge capability contract with** — Extend the Forge capability contract with remote mutations.
- **External adapters** — External adapters
- **External projections Projection Adapters** — External projections (Projection Adapters)
- **External-system integration adapters GitHub** — External-system integration adapters (GitHub).
- **Filesystem Capability Interface** — 1. Filesystem Capability Interface
- **Filesystem Observation Capability** — Filesystem Observation Capability
- **FilesystemAdapter over a knowledge directory** — `FilesystemAdapter` over a `./knowledge` directory
- **Finding 1 Adapter proliferation** — Finding 1 — Adapter proliferation
- **Finding 2 Environment capability framework vs** — Finding 2 — Environment capability framework vs. adapters
- **First Contact first-contact- test js 13** — 4. First Contact (`first-contact-*.test.js`) — 13 files
- **First Operator Experience test passes tests** — First Operator Experience test passes (`tests/first-operator-experience.test.js`)
- **First-contact adapters conversation adapter** — **First-contact adapters** (`conversation` adapter).
- **Fixed src mission-studio adapter-observation-collector ts enrichment** — Fixed `src/mission-studio/adapter-observation-collector.ts` enrichment adapter `this` binding.
- **Fixed src mission-studio adapter-observation-collector ts to** — Fixed `src/mission-studio/adapter-observation-collector.ts` to invoke enrichment adapters with explicit `this` binding (`enrich.call(adapter, rawObservations)`), preventing `undefined` context errors during bootstrap analysis.
- **Flush state to StateStore** — Flush state to StateStore
- **Forge Adapter Contract** — Forge Adapter Contract
- **Forge Capability Interface** — 1. Forge Capability Interface
- **ForgeAdapter interface in src repository forge-adapter** — `ForgeAdapter` interface in `src/repository/forge-adapter.ts`.
- **Freeze capability registry** — Freeze capability registry
- **fs existsSync adapters tdd adapter ts** — `fs.existsSync` — adapters/tdd/adapter.ts, adapters/bdd/adapter.ts, adapters/filesystem/adapter.ts, adapters/specification/adapter.ts, adapters/document/adapter.ts, adapters/repository/git.ts
- **fs mkdirSync adapters bdd adapter ts** — `fs.mkdirSync` — adapters/bdd/adapter.ts, adapters/repository/git.ts
- **fs readdirSync adapters bdd adapter ts** — `fs.readdirSync` — adapters/bdd/adapter.ts, adapters/filesystem/adapter.ts, adapters/repository/git.ts, adapters/specification/adapter.ts, adapters/document/adapter.ts
- **fs readFileSync adapters repository git ts** — `fs.readFileSync` — adapters/repository/git.ts, adapters/filesystem/adapter.ts, adapters/specification/adapter.ts, adapters/document/adapter.ts, adapters/bdd/adapter.ts
- **fs writeFileSync adapters bdd adapter ts** — `fs.writeFileSync` — adapters/bdd/adapter.ts, adapters/tdd/adapter.ts, adapters/repository/git.ts
- **Future adapters** — 1.3 Future adapters
- **Future adapters may be added for** — Future adapters may be added for GitLab, Azure, Jenkins.
- **Future capability additions must include failure** — Future capability additions must include failure and recovery certification.
- **Future Capability Roadmap** — Future Capability Roadmap
- **Generate test skeletons** — Generate test skeletons
- **Generic Projection Capability** — Generic Projection Capability
- **Generic VersioningCapability interface defining repository-versioning operations** — Generic `VersioningCapability` interface defining repository-versioning operations.
- **Genesis genesis- test js 2 files** — 5. Genesis (`genesis-*.test.js`) — 2 files
- **Git Observation Capability** — Git Observation Capability
- **GitAdapter GitMutationAdapter** — `GitAdapter` → `GitMutationAdapter`
- **GitAdapterImpl being instantiated when gitEnabled is** — `GitAdapterImpl` being instantiated when `gitEnabled` is true.
- **GitHub Actions adapter runs npm run** — GitHub Actions adapter runs `npm run govern` on PR and push.
- **GitRepositoryAdapter implements observe without mutating state** — `GitRepositoryAdapter` implements `observe()` without mutating state.
- **GitRepositoryAdapter keep Mission Studio domain** — `GitRepositoryAdapter` → keep (Mission Studio domain)
- **Governance applies uniformly across all adapters** — Governance applies uniformly across all adapters.
- **Governance artifact paths referenced by tests** — Governance artifact paths referenced by tests and certification reports.
- **Governance does not implement adapters it** — Governance does not implement adapters; it consumes the `Observation` values adapters produce.
- **Governance Engine govern- test js governance-** — 6. Governance Engine (`govern-*.test.js`, `governance-*.test.js`) — 8 files
- **Governance policy evaluation permits and attestation** — 08 - Governance — policy evaluation, permits, and attestation.
- **Governance-related tests under tests governance- or** — Governance-related tests under `tests/governance-*` or equivalent
- **Governed adapters participate in governance but** — **Governed** — adapters participate in governance but never bypass it.
- **GraphQL files produce evidence and capability** — GraphQL files produce evidence and capability observations.
- **Greenfield onboarding becomes teachable testable and** — Greenfield onboarding becomes teachable, testable, and certifiable.
- **Hand-written certification tests only** — 1. Hand-written certification tests only
- **Harder Tests must cover multiple provider** — **Harder:** Tests must cover multiple provider configurations and in-memory environments.
- **Has a capability name** — Has a capability name
- **Heuristics breaking decisions major new capability** — Heuristics: breaking decisions → major, new capability missions/expeditions → minor, completed expeditions → patch.
- **Homepage acceptance test** — 7. Homepage acceptance test
- **Homepage Runtime adapter** — 1.2 Homepage Runtime adapter
- **How to Add a Capability** — How to Add a Capability
- **Human operator testing demonstrates successful onboarding** — Human operator testing demonstrates successful onboarding. (Pending live cohorts)
- **Hybrid contract-driven rules drift-class adapters** — Hybrid: contract-driven rules + drift-class adapters
- **I4 Projection adapters isolate external tooling** — I4: Projection adapters isolate external tooling.
- **I5 Capability registry is immutable after** — **I5:** Capability registry is immutable after seal
- **I5 Immutable Capability Graph** — I5: Immutable Capability Graph
- **I5 The capability graph shall be** — I5: The capability graph shall be immutable after seal
- **Identity Capability Interface** — 2. Identity Capability Interface
- **If the capability is not registered** — If the capability is not registered, the dispatch returns a noop (empty event set)
- **If the capability is registered and** — If the capability is registered and policy allows, the domain logic is executed and events are emitted
- **If the capability is registered but** — If the capability is registered but policy denies it, the dispatch is rejected with a PolicyBlockedError
- **Impact analysis validation planning capability mapping** — Impact analysis, validation planning, capability mapping, protected-asset escalation, and CLI/CI integration are now operational.
- **Implement a GitObservationCapability that reads git** — Implement a `GitObservationCapability` that reads `.git/` metadata without modifying the repository.
- **Implement adapters that observe external systems** — Implement adapters that observe external systems and emit `Observation` values.
- **Implement GitVersioningProvider satisfying VersioningCapability** — Implement `GitVersioningProvider` satisfying `VersioningCapability`.
- **Implementation of the capability** — Implementation of the capability.
- **Implementation src environment capability-report ts buildCapabilityReport** — **Implementation:** `src/environment/capability-report.ts` — `buildCapabilityReport(evidence)` projects `synth-capability-report-v1` from discovery evidence (status/provider/confidence/reason per family, `unavailable` quick-scan list, assumptions, embedded planning guidance) and `renderCapabilityReportMarkdown(report)` renders the agent-consumable document. Exported via `src/environment/index.ts`.
- **Implementation src environment forge-capability ts defines** — **Implementation:** `src/environment/forge-capability.ts` defines the `ForgeProvider` interface plus forge-agnostic `ForgeRepository`/`ForgeIssue`/`ForgePullRequest`/`ForgeRelease`/`ForgeListOptions` abstractions, and the `GitHubForgeProvider` reference implementation. The provider composes the `ToolProvider` capability (ADR-011) via the `gh` CLI; authentication is delegated to `gh` — no credential handling in the provider. Read-only scope per ADR-013. Exported via `src/environment/index.ts`.
- **Implementation src environment process-capability ts defines** — **Implementation:** `src/environment/process-capability.ts` defines `ProcessProvider` and `ToolProvider` interfaces, `ProcessRequest`/`ProcessResult`/`ToolRunOptions` data types, and the `LocalShellProvider` reference implementation (Node.js `child_process.spawn`, no shell interpolation, errors-as-data results). Exported via `src/environment/index.ts`.
- **Implementation src environment runtime-capability ts defines** — **Implementation:** `src/environment/runtime-capability.ts` defines `RuntimeProvider` and `PackageProvider` interfaces with `RuntimeInfo`/`PackageRequest`/`PackageInfo` data types, plus the `NodeRuntimeProvider` and `NpmPackageProvider` reference implementations. Both providers compose the `ToolProvider` capability from ADR-011 — no direct `child_process` usage. Exported via `src/environment/index.ts`.
- **Implementation src environment secrets-capability ts defines** — **Implementation:** `src/environment/secrets-capability.ts` defines `SecretsProvider` and `IdentityProvider` interfaces with `IdentityInfo`/`EnvMap` data types, and the `EnvVarProvider` reference implementation (injectable env map, defaults to `process.env`; identity resolved by `GIT_AUTHOR_*` convention with `USER`/`GITHUB_ACTOR` fallbacks; CI detection; name-only secret discovery via documented naming heuristic). Exported via `src/environment/index.ts`.
- **Implemented src adapters github adapter ts** — Implemented `src/adapters/github/adapter.ts` — `GitHubAdapterImpl` with canonical lifecycle and capabilities:
- **Implemented src adapters github client ts** — Implemented `src/adapters/github/client.ts` — thin, mockable fetch-based GitHub API client.
- **Implemented src adapters tdd adapter ts** — Implemented `src/adapters/tdd/adapter.ts` — `TddAdapterImpl` with canonical lifecycle and capabilities:
- **Implemented src adapters tdd types ts** — Implemented `src/adapters/tdd/types.ts` — TDD-specific types, workflow states, and evidence model.
- **Implemented src environment filesystem-capability ts** — Implemented `src/environment/filesystem-capability.ts`:
- **Implemented src environment revision-capability ts** — Implemented `src/environment/revision-capability.ts`:
- **Implemented src environment workspace-capability ts** — Implemented `src/environment/workspace-capability.ts`:
- **Implementing live AI adapters Phase 3** — Implementing live AI adapters (Phase 3).
- **Infrastructure Adapters** — Infrastructure Adapters
- **Initial failing tests** — Initial failing tests
- **Initialization replay tests pass in CI** — Initialization replay tests pass in CI.
- **InMemorySnapshotStore for tests and ephemeral use** — `InMemorySnapshotStore` — for tests and ephemeral use
- **Installer contract tests pass** — Installer contract tests pass
- **Integration tests** — 1.3 Integration tests
- **Integration Tests 5 tests** — Integration Tests (5 tests)
- **Integration tests pass for Mission Studio** — Integration tests pass for Mission Studio scroll, phase, artifact, and synchronization behavior.
- **Integration tests run in CI** — Integration tests run in CI.
- **Intent payload capability field intent type** — Intent payload: `capability` field → `intent.type` nested field
- **Interaction Capability** — Interaction Capability
- **Introduce deterministic adapter classification deterministic contextual** — Introduce **deterministic adapter classification**: `deterministic`, `contextual`, `non-deterministic`.
- **Introduce the Capability Graph** — 1. Introduce the Capability Graph
- **Keep adapters confined to the Acquire** — Keep adapters confined to the Acquire stage.
- **knowledge-extraction confidence dependency and architecture adapters** — `knowledge-extraction`, `confidence`, `dependency`, and `architecture` adapters overlap with **Discovery**, **Knowledge**, and **Verification** subsystems.
- **latest and stable resolve via npm** — `latest` and `stable` resolve via `npm view @synth-framework/synth version`.
- **Latest proof artifact proof proof-2026-07-12T07-21-11-599Z json** — Latest proof artifact (`proof/proof-2026-07-12T07-21-11-599Z.json`)
- **Latest verified proof artifact reference** — Latest verified proof artifact reference
- **Layer 1 Evidence Adapters** — Layer 1 — Evidence Adapters
- **Layer 2 Adapters** — Layer 2: Adapters
- **Layer 2 Intelligence Adapters** — Layer 2 — Intelligence Adapters
- **Layer 3 Planning Adapters** — Layer 3 — Planning Adapters
- **Layer 4 Tests 2 tests** — Layer 4 Tests (2 tests)
- **Layer 5 Tests 3 tests** — Layer 5 Tests (3 tests)
- **Load scenarios from tests certifications or** — Load scenarios from `tests/certifications/` or `certifications/`.
- **Manifest created at docs reference capability-validation-map** — Manifest created at `docs/reference/capability-validation-map.json` with schema `synth-capability-validation-map-v1`.
- **Manifest schema v1 defined with latest** — Manifest schema v1 defined with `latest` and `stable` channels.
- **Many adapters slice the digest to** — Many adapters slice the digest to 8, 12, or 16 characters
- **Maps src adapters name to Name** — Maps `src/adapters/<name>/` to `<Name>Adapter`, `src/mission-studio/` to `MissionStudio`, etc.
- **Materialization is gated by capability verification** — Materialization is gated by capability verification.
- **May describe adapters for GitHub Issues** — May describe adapters for GitHub Issues, Jira, Linear, Azure DevOps
- **Merge capability provider taxonomy into src** — Merge capability/provider taxonomy into `src/discovery/types.ts`.
- **Merge src adapters registry ts into** — Merge `src/adapters/registry.ts` into `src/discovery/adapter-registry.ts` or delete it if no consumer needs name-based lookup.
- **Migrate adapters and CLI preserve kernel** — Migrate adapters and CLI; preserve kernel `computeEventHash` semantics.
- **Migrate execSync spawn usages in adapters** — Migrate `execSync`/`spawn` usages in adapters and CLI.
- **MINOR New capability within the existing** — **MINOR** — New capability within the existing public contract.
- **Missing capability** — Missing capability
- **Mission Projection specification and capability remain** — Mission Projection specification and capability remain correct.
- **Mission Studio Adapter Ecosystem** — Mission Studio Adapter Ecosystem
- **Mission Studio mission-studio-adapter-collector mission-studio-adapter-mapper** — Mission Studio (`mission-studio-adapter-collector`, `mission-studio-adapter-mapper`)
- **mission-builder expedition-builder objective-builder and wizard adapters** — `mission-builder`, `expedition-builder`, `objective-builder`, and `wizard` adapters produce plans that the **Planning Cognition Engine** and **Mission Studio** also produce.
- **Model A src adapters AdapterRegistry** — Model A — `src/adapters/` (AdapterRegistry)
- **Model B src environment DiscoveryOrchestrator CapabilityProvider** — Model B — `src/environment/` (DiscoveryOrchestrator + CapabilityProvider)
- **Modifying test code that consumes the** — Modifying test code that consumes the JSON artifacts (tests use paths from the artifacts).
- **Modifying the kernel EventStore StateStore Replay** — Modifying the kernel, EventStore, StateStore, Replay, or ExecutionGate.
- **Move buildCapabilityReport to discovery projections** — Move `buildCapabilityReport` to discovery projections.
- **move-to-adapter belongs to implementation not governance** — **move-to-adapter** — belongs to implementation, not governance
- **Must include attestation hashes in every** — Must include attestation hashes in every decision
- **Mutable adapter configuration via info** — Mutable adapter configuration via `info`.
- **New adapter implementations** — New adapter implementations
- **New adapters can be introduced without** — New adapters can be introduced without modifying the Kernel.
- **New capability providers may be introduced** — New capability providers may be introduced without modification to constitutional behavior.
- **New ExecutionArtifactAdapter lines 197-252** — New: ExecutionArtifactAdapter (lines 197-252)
- **New guards are wired into test** — New guards are wired into `test:all`.
- **New regression tests tests initialization-evidence test** — New regression tests: `tests/initialization-evidence.test.js` and `tests/initialization-replay.test.js`.
- **New Test Categories Required** — New Test Categories Required
- **No adapter introspection N5 There is** — **No adapter introspection (N5).** There is no `synth adapter info`; capabilities must be discovered by reading source.
- **No dedicated test script in test** — No dedicated test script in `test:all`.
- **No duplicate Mission Studio adapters** — No duplicate Mission Studio adapters.
- **No existing tests regress** — No existing tests regress.
- **No kernel modifications EventStore StateStore Replay** — No kernel modifications (EventStore, StateStore, Replay, ExecutionGate remain untouched).
- **No new tests** — No new tests.
- **No platform-specific concepts leak beyond adapters** — No platform-specific concepts leak beyond adapters.
- **No state outside the adapter is** — No state outside the adapter is modified.
- **No test deletion** — No test deletion.
- **Nodes every capability observation subject** — Nodes: every `capability` observation subject.
- **Note The product-boundary GitHubAdapter in src** — **Note:** The product-boundary `GitHubAdapter` in `src/adapters/github/` is unchanged; the Forge capability is an Environment Layer artifact, not a replacement for the adapter's product-level functions.
- **npm remains a supported adapter this** — npm remains a supported adapter; this ADR does not remove npm from the project.
- **npm run test ai-benchmark PASS** — `npm run test:ai-benchmark` — PASS
- **npm run test all** — `npm run test:all`
- **npm run test all PASS** — `npm run test:all` — PASS
- **npm run test all PASS all** — `npm run test:all` — PASS (all suites green)
- **npm run test all passes** — `npm run test:all` passes.
- **npm run test all passes 113** — `npm run test:all` passes 113 tests.
- **npm run test all reports 202** — `npm run test:all` reports 202 passing tests.
- **npm run test audit passes** — `npm run test:audit` passes.
- **npm run test audit passes with** — `npm run test:audit` passes with zero file exemptions.
- **npm run test brownfield npm run** — `npm run test:brownfield`, `npm run test:adoption-validation`, `npm run ai:benchmark:dry-run`.
- **npm run test certification-framework after editing** — `npm run test:certification-framework` after editing certification source code.
- **npm run test determinism passes** — `npm run test:determinism` passes.
- **npm run test freeze-certification 10 10** — `npm run test:freeze-certification` — 10/10 checks pass.
- **npm run test freeze-certification passes** — `npm run test:freeze-certification` passes.
- **npm run test or npm run** — `npm run test` or `npm run test:all` (the full test suite).
- **npm run test public-vocabulary-audit PASS** — `npm run test:public-vocabulary-audit` — PASS
- **npm run test public-vocabulary-audit passes** — `npm run test:public-vocabulary-audit` passes.
- **npm run test replay passes** — `npm run test:replay` passes.
- **npm run test replay passes and** — `npm run test:replay` passes and reports correct projection counts for the current domain.
- **npm run test replay reports Chain** — `npm run test:replay` reports `Chain valid: ✅`.
- **npm run test replay-graph-integrity after editing** — `npm run test:replay-graph-integrity` after editing `scripts/verify-replay.js` or replay-related code.
- **npm script test environment-capability-report included in** — **npm script:** `test:environment-capability-report`, included in `test:all`.
- **npm script test environment-discovery-evidence included in** — **npm script:** `test:environment-discovery-evidence`, included in `test:all`.
- **npm script test environment-forge included in** — **npm script:** `test:environment-forge`, included in `test:all`.
- **npm script test environment-process included in** — **npm script:** `test:environment-process`, included in `test:all`.
- **npm script test environment-runtime included in** — **npm script:** `test:environment-runtime`, included in `test:all`.
- **npm script test environment-secrets included in** — **npm script:** `test:environment-secrets`, included in `test:all`.
- **npm scripts Added test brownfield test** — **npm scripts** — Added `test:brownfield`, `test:adoption-validation`, and `ai:benchmark:dry-run`; all wired into `npm run test:all`.
- **npm scripts become a thin adapter** — npm scripts become a thin adapter, not the workflow engine.
- **npm scripts test first-contact-projection wired into** — npm scripts: `test:first-contact-projection` (wired into `test:all`), `docs:generate-first-contact`, `docs:verify-first-contact-projection`.
- **npm test passes** — `npm test` passes.
- **npm test passes 121 passed 0** — `npm test` passes (121 passed, 0 failed).
- **Observable Adapter Interface** — Observable Adapter Interface
- **ObservableAdapter is part of the adapter** — `ObservableAdapter` is part of the adapter constitution.
- **Observation canonical planning input adapter-agnostic** — **Observation** — canonical planning input, adapter-agnostic.
- **Observation Capability Contract** — Observation Capability Contract
- **Observation capability contract and correlation rules** — Observation capability, contract, and correlation rules.
- **ObservationCapability contract** — ObservationCapability contract
- **OpenAPI files produce evidence and capability** — OpenAPI files produce evidence and capability observations.
- **Operational artifact observation adapter** — Operational artifact observation adapter.
- **Operator adapter interface** — Operator adapter interface
- **Operator journey certification report data-test operator-journey-certification** — Operator journey certification report (`data-test/operator-journey-certification.json`)
- **Optionally enables the Git observation capability** — Optionally enables the Git observation capability if `.git` is present.
- **Orchestrating adapter observation collection through the** — Orchestrating adapter observation collection through the Adapter Registry
- **Ordering Tests 2 tests** — Ordering Tests (2 tests)
- **Outcome 1 InitializationAdapter Contract** — Outcome 1 — InitializationAdapter Contract
- **Outcome 2 Filesystem Initialization Adapter** — Outcome 2 — Filesystem Initialization Adapter
- **Outcome 4 Governance Tests** — Outcome 4 — Governance Tests
- **Outcome 6 Regression Test Suite** — Outcome 6 — Regression Test Suite
- **Outcome 7 Initialization Replay Tests** — Outcome 7 — Initialization Replay Tests
- **Output adapter metadata current lifecycle state** — Output adapter metadata, current lifecycle state, and health.
- **Outputs CapabilityVerificationReport** — 1.3 Outputs — `CapabilityVerificationReport`
- **P0 Structural Tests 10 tests** — P0 Structural Tests (10 tests)
- **P1 Attestation Tests 6 tests** — P1 Attestation Tests (6 tests)
- **P1 Cryptographic attestation** — **P1:** Cryptographic attestation
- **P1 Structural Verified by test audit** — **P1 Structural:** Verified by `test:audit` and proof generation.
- **P2 Behavioral Verified by test replay** — **P2 Behavioral:** Verified by `test:replay` and `test:determinism`.
- **P2 Hardening Tests 5 tests** — P2 Hardening Tests (5 tests)
- **P3 Historical Strengthened the capability graph** — **P3 Historical:** Strengthened — the capability graph is preserved as replayable evidence.
- **P4 Adversarial Verified by test adversarial** — **P4 Adversarial:** Verified by `test:adversarial`.
- **Package Capability Interface** — 2. Package Capability Interface
- **package json added test freeze-certification and** — `package.json` — added `test:freeze-certification` and wired it into `test:all`.
- **package json added test public-vocabulary-audit and** — `package.json` — added `test:public-vocabulary-audit` and wired it into `test:all`.
- **package json test environment-versioning script wired** — `package.json` — `test:environment-versioning` script wired into `test:all`.
- **package json test environment-versioning-certification script wired** — `package.json` — `test:environment-versioning-certification` script wired into `test:all`.
- **Passing tests** — Passing tests
- **Performance tests** — 1.7 Performance tests
- **Performance tests meet FCP LCP TTI** — Performance tests meet FCP, LCP, TTI, FPS, bundle, and lazy-loading budgets.
- **Phase 1 Fixture and failing tests** — Phase 1 — Fixture and failing tests
- **Phase 1 Inventory existing tests** — Phase 1 — Inventory existing tests
- **Phase 2 Add relationship tests** — Phase 2 — Add relationship tests
- **Phase 2 Build Test Missions** — Phase 2 — Build Test Missions
- **Phase 2 Shared Test Infrastructure** — Phase 2 — Shared Test Infrastructure ✅
- **Phase 3 CI Adapters** — Phase 3 — CI Adapters
- **Phase 3 Global install smoke test** — Phase 3 — Global install smoke test
- **Phase 3 Synchronization test** — Phase 3 — Synchronization test
- **Phase 4 Capability Verification** — Phase 4: Capability Verification
- **Phase 4 npx smoke test** — Phase 4 — npx smoke test
- **Phase 4 Regression tests** — Phase 4 — Regression tests
- **Phase 4 Run comprehension tests** — Phase 4 — Run comprehension tests
- **Phase 8 Tests** — Phase 8: Tests
- **Planning intelligence adapters mission-builder expedition-builder objective-builder** — Planning/intelligence adapters (mission-builder, expedition-builder, objective-builder, wizard, tdd, bdd, conversation, document, specification, dependency, architecture).
- **Platform Adapters** — Platform Adapters
- **Policy Attestation** — Policy Attestation
- **Policy attestation hash comparison** — Policy attestation hash comparison
- **Policy attestation returns hash** — Policy attestation returns hash
- **Policy changes are detectable attestation hash** — Policy changes are detectable (attestation hash changes)
- **Policy changes do not require capability** — Policy changes do not require capability changes
- **Policy conditions intent capability intent type** — Policy conditions: `intent.capability` → `intent.type`
- **Policy evaluation is logged and attested** — Policy evaluation is logged and attested; capability execution is not
- **Policy Tests 3 tests** — Policy Tests (3 tests)
- **Preserve full adapter provenance in the** — Preserve full adapter provenance in the session.
- **Prevent implementation before failing tests** — Prevent implementation before failing tests
- **Principle 8 Capability Isolation** — Principle 8: Capability Isolation
- **Process Capability Interface** — 1. Process Capability Interface
- **Producing what attestation** — Producing what attestation
- **Production certification tests pass unit integration** — Production certification tests pass (unit, integration, E2E, visual regression, accessibility, performance).
- **Project one canonical AI Capability Model** — Project one canonical AI Capability Model into platform-specific skills and rules.
- **Projection adapters can evolve without affecting** — Projection adapters can evolve without affecting canonical knowledge
- **Projection capability contract** — Projection capability contract
- **Projection Contract Tests** — 4. Projection Contract Tests
- **Projection Layer external-system projections adapters this** — Projection Layer — external-system projections (adapters); this document governs internal projections.
- **proof artifacts for capability verification** — proof artifacts for capability verification
- **Proposal Evaluation Capability Design Document** — Proposal Evaluation Capability — Design Document
- **Proposed agent capability model** — Proposed agent capability model
- **Prototype-first validation beyond capability checks EXP-PROGRAM-025** — Prototype-first validation beyond capability checks (EXP-PROGRAM-025).
- **Prove cross-run equivalence for deterministic adapters** — Prove **cross-run equivalence** for deterministic adapters.
- **provenance versioned adapter identifier** — `provenance` — versioned adapter identifier.
- **provenance which adapter and inputs produced** — `provenance` — which adapter and inputs produced each node.
- **Published-package smoke test passed on macOS** — Published-package smoke test passed on macOS:
- **Q2 What does the capability evaluate** — Q2 — What does the capability evaluate against?
- **Q3 What does the capability produce** — Q3 — What does the capability produce?
- **Q4 Where does the capability live** — Q4 — Where does the capability live?
- **Re-implements language framework and test detection** — Re-implements language, framework, and test detection.
- **README passes a first-time reader test** — README passes a first-time reader test: an independent reviewer can explain SYNTH in one sentence.
- **Recommendation 6 Determinism Test** — Recommendation 6: Determinism Test
- **Refactor language framework runtime lifecycle capability** — Refactor language, framework, runtime, lifecycle, capability, knowledge inventory, and unknown inference into discrete `ProjectModelRule`s.
- **Refactor the existing filesystem adapter into** — Refactor the existing filesystem adapter into a `FilesystemObservationCapability`.
- **Registered adapters repository github tdd bdd** — Registered adapters: repository, github, tdd, bdd, conversation, document, filesystem, specification, knowledge-extraction, confidence, dependency, architecture, mission-builder, expedition-builder, objective-builder, wizard.
- **Registered test installer-certification in package json** — Registered \`test:installer-certification\` in \`package.json\` and \`test:all\` (intended for CI).
- **Registered test installer-engine in package json** — Registered `test:installer-engine` in `package.json` and included it in `test:all`.
- **Registered test installer-upgrade in package json** — Registered `test:installer-upgrade` in `package.json` and `test:all`.
- **Registered test installer-verify in package json** — Registered `test:installer-verify` in `package.json` and included it in `test:all`.
- **Registered the adapter in src adapters** — Registered the adapter in `src/adapters/registry.ts`.
- **Registry and factory pattern for adapter** — Registry and factory pattern for adapter registration.
- **Regression guards added in tests extraction-reporting** — Regression guards added in `tests/extraction-reporting.test.js` and wired into `test:all`.
- **Regression guards tests governance-record test js** — **Regression guards** — `tests/governance-record.test.js` covers projection-module derivation, CLI lineage rendering, and empty-log handling; wired into `test:all` as `test:governance-record`.
- **Regression test tests governance execution-gate-regression test** — **Regression test:** `tests/governance/execution-gate-regression.test.js` passes and includes a new `testGenesisRejectsOperationalEvents` case.
- **Regression tests** — 1.3 Regression tests
- **Regression tests for integrity OK and** — Regression tests for integrity OK and tamper detection.
- **Regression tests for known adapters and** — Regression tests for known adapters and unknown-adapter error handling.
- **Regression tests for normal extraction and** — Regression tests for normal extraction and zero-extraction warning.
- **Regression tests for root repository fresh** — Regression tests for root repository, fresh project, and unclassified directory.
- **Regression tests in tests resume-briefing test** — Regression tests in `tests/resume-briefing.test.js`.
- **Regression tests prove the boundary cannot** — Regression tests prove the boundary cannot be bypassed.
- **Regression tests proving clean output for** — Regression tests proving clean output for commands that bootstrap.
- **Relationship to the existing Revision capability** — Relationship to the existing `Revision` capability (read-only observation) and the `Forge` capability (remote collaboration).
- **Relationship to unit tests and governance** — Relationship to unit tests and governance proofs
- **Release process is documented and tested** — Release process is documented and tested.
- **Remain an application-level capability built on** — Remain an application-level capability built on the frozen SYNTH kernel.
- **Remove existing adapters used by Mission** — Remove existing adapters used by Mission Studio.
- **Removing every Mission Adapter leaves the** — Removing every Mission Adapter leaves the kernel unchanged.
- **Rename Capability Intent at architectural layer** — Rename (Capability → Intent at architectural layer)
- **Replace unit tests or integration tests** — Replace unit tests or integration tests.
- **Replaceable swapping one adapter for another** — **Replaceable** — swapping one adapter for another must not require Kernel changes.
- **Replay fingerprints tests and audits are** — Replay, fingerprints, tests, and audits are reproducible.
- **Replay graph integrity tests pass resolver** — Replay graph integrity tests pass; resolver output is deterministic.
- **Replay State Tests 7 tests** — Replay/State Tests (7 tests)
- **Repository governance capabilities in src capability** — Repository governance capabilities in `src/capability/registry.ts`: `CreateBranch`, `OpenPullRequest`, `MergePullRequest`, `CreateRelease`, `ApprovePromotion`.
- **RepositoryAdapter interface declares observe Promise ObservationBatch** — `RepositoryAdapter` interface declares `observe(): Promise<ObservationBatch>`.
- **Require operator approval for every test** — 3. Require operator approval for every test command
- **Requires adapters for each supported forge** — Requires adapters for each supported forge.
- **Requires conformance tests for SKR compliance** — Requires conformance tests for SKR compliance
- **requires edges capability capability a capability** — **`requires` edges** — capability → capability: a capability depends on another (for example, Forge requires Revision and Network; Revision requires Filesystem).
- **Requires evidence from testing** — Requires evidence from testing
- **Requires existing tests and capabilities to** — Requires existing tests and capabilities to be audited for conformance.
- **Resolve capability names to capability definitions** — Resolve capability names to capability definitions
- **Return ALLOW or DENY with attestation** — Return ALLOW or DENY with attestation hashes
- **Return CapabilityResult containing events** — Return `CapabilityResult` containing events.
- **Returns ALLOW DENY with attestation** — Returns ALLOW/DENY with attestation
- **Reuse an existing test framework e** — 2. Reuse an existing test framework (e.g., Jest, Mocha)
- **Revision Capability Interface** — 1. Revision Capability Interface
- **Rollback plan is tested and documented** — Rollback plan is tested and documented.
- **Rule model contract-driven rules drift-class adapters** — Rule model: contract-driven rules + drift-class adapters
- **Run the test suite** — Run the test suite
- **Running Certification Tests** — Running Certification Tests
- **Runtime capability checks confirm feasibility before** — Runtime capability checks confirm feasibility before Mission approval.
- **Runtime Capability Interface** — 1. Runtime Capability Interface
- **Runtime capability verification EXP-KNOWLEDGE-002** — Runtime capability verification (EXP-KNOWLEDGE-002).
- **Runtime tests packages homepage-runtime src runtime** — Runtime tests: `packages/homepage-runtime/src/runtime.test.js`
- **satisfies edges provider capability the provider** — **`satisfies` edges** — provider → capability: the provider can satisfy the capability.
- **Seal freezes capability registry** — Seal freezes capability registry
- **Secrets Capability Interface** — 1. Secrets Capability Interface
- **Security analysis can be performed per-capability** — Security analysis can be performed per-capability
- **Session create-new-capability evidence** — Session `create-new-capability` — evidence
- **Skeleton GitHubAdapter in src repository adapters** — Skeleton `GitHubAdapter` in `src/repository/adapters/github-adapter.ts`.
- **Snapshot Lineage Test** — Snapshot Lineage Test
- **Snapshot lineage tests and reports** — Snapshot lineage tests and reports
- **source which adapter or operator input** — `source` — which adapter or operator input produced the node.
- **sourceAdapter which adapter produced it** — `sourceAdapter` — which adapter produced it
- **Specification capabilities produce capability observations** — Specification capabilities produce capability observations.
- **src adapters** — src/adapters/
- **src adapters architecture adapter ts implements** — `src/adapters/architecture/adapter.ts` implements the inference logic.
- **src adapters architecture types ts defines** — `src/adapters/architecture/types.ts` defines the contract.
- **src adapters bdd adapter ts BddAdapterImpl** — `src/adapters/bdd/adapter.ts` — `BddAdapterImpl` with canonical lifecycle and capabilities.
- **src adapters bdd types ts BddFeature** — `src/adapters/bdd/types.ts` — `BddFeature`, `BddScenario`, `BddEvidence`, and capability result contracts.
- **src adapters confidence adapter ts implements** — `src/adapters/confidence/adapter.ts` implements the evaluation logic.
- **src adapters confidence types ts defines** — `src/adapters/confidence/types.ts` defines the contract.
- **src adapters contains 16 registered adapters** — `src/adapters/` contains **16 registered adapters** and **~5,650 lines** of TypeScript.
- **src adapters conversation adapter ts implements** — `src/adapters/conversation/adapter.ts` implements `ObservableAdapter`.
- **src adapters conversation types ts defines** — `src/adapters/conversation/types.ts` defines the input/output contracts.
- **src adapters dependency adapter ts implements** — `src/adapters/dependency/adapter.ts` implements the graph construction logic.
- **src adapters dependency types ts defines** — `src/adapters/dependency/types.ts` defines the contract.
- **src adapters document adapter ts implements** — `src/adapters/document/adapter.ts` implements `ObservableAdapter`.
- **src adapters document types ts defines** — `src/adapters/document/types.ts` defines the input/output contracts.
- **src adapters expedition-builder adapter ts implements** — `src/adapters/expedition-builder/adapter.ts` implements the expedition derivation logic.
- **src adapters expedition-builder types ts defines** — `src/adapters/expedition-builder/types.ts` defines the contract.
- **src adapters external-system adapters** — `src/adapters/` — external-system adapters
- **src adapters filesystem adapter ts implements** — `src/adapters/filesystem/adapter.ts` implements `ObservableAdapter`.
- **src adapters filesystem types ts defines** — `src/adapters/filesystem/types.ts` defines the input/output contracts.
- **src adapters github adapter ts GitHubAdapterImpl** — `src/adapters/github/adapter.ts` — `GitHubAdapterImpl implements GitHubAdapter` (Mission Studio integration: health, lifecycle, issues, PRs)
- **src adapters github adapter ts vs** — `src/adapters/github/adapter.ts` vs `src/repository/adapters/github-adapter.ts`
- **src adapters knowledge adapter ts implements** — `src/adapters/knowledge/adapter.ts` implements the extraction logic.
- **src adapters knowledge types ts defines** — `src/adapters/knowledge/types.ts` defines the contract.
- **src adapters mission-builder adapter ts implements** — `src/adapters/mission-builder/adapter.ts` implements the mission derivation logic.
- **src adapters mission-builder types ts defines** — `src/adapters/mission-builder/types.ts` defines the contract.
- **src adapters objective-builder adapter ts implements** — `src/adapters/objective-builder/adapter.ts` implements the objective derivation logic.
- **src adapters objective-builder types ts defines** — `src/adapters/objective-builder/types.ts` defines the contract.
- **src adapters registry ts registered as** — `src/adapters/registry.ts` — registered as `"bdd"` methodology adapter.
- **src adapters repository git ts GitRepositoryAdapter** — `src/adapters/repository/git.ts` — `GitRepositoryAdapter implements RepositoryAdapter`: Mission Studio integration adapter with lifecycle, health, observations
- **src adapters repository git ts mutation** — `src/adapters/repository/git.ts` (mutation methods) vs `src/infra/git-adapter.js`
- **src adapters repository git ts repository** — `src/adapters/repository/git.ts` — repository adapter.
- **src adapters specification adapter ts implements** — `src/adapters/specification/adapter.ts` implements `ObservableAdapter`.
- **src adapters specification types ts defines** — `src/adapters/specification/types.ts` defines the input/output contracts.
- **src capability registry ts** — `src/capability/registry.ts`
- **src capability registry ts 13 46** — `src/capability/registry.ts:13–46` — `register()` has no `_frozen` check; no `freeze()` method.
- **src capability registry ts 51 name** — `src/capability/registry.ts:51` — `name: "CreateTicket"`, `description: "Create a new ticket"`.
- **src capability registry ts added missing** — `src/capability/registry.ts` — added missing `CompleteObjective` capability discovered during certification.
- **src capability registry ts Capability registry** — `src/capability/registry.ts` — Capability registry
- **src capability registry ts RecordRepair** — `src/capability/registry.ts` (`RecordRepair`)
- **src capability registry ts RecordRepair capability** — `src/capability/registry.ts` — `RecordRepair` capability.
- **src cli adapter ts added bdd-create-feature** — `src/cli/adapter.ts` — added `bdd-create-feature`, `bdd-create-scenario`, `bdd-generate-tests`, `bdd-verify`, `bdd-evidence` CLI commands.
- **src discovery adapter-registry ts Discovery adapters** — `src/discovery/adapter-registry.ts` — Discovery adapters
- **src discovery adapters filesystem-adapter ts** — `src/discovery/adapters/filesystem-adapter.ts`
- **src environment forge-capability ts** — `src/environment/forge-capability.ts`
- **src environment forge-capability ts extended ForgeProvider** — `src/environment/forge-capability.ts` — extended `ForgeProvider` interface with `createPullRequest`, `mergePullRequest`, and `forkRepository`.
- **src environment forge-capability ts remote forge** — `src/environment/forge-capability.ts` (remote forge abstraction)
- **src environment process-capability ts how mutations** — `src/environment/process-capability.ts` (how mutations are executed)
- **src environment providers environment capability providers** — `src/environment/providers/` — environment capability providers
- **src environment revision-capability ts** — `src/environment/revision-capability.ts`
- **src environment revision-capability ts read-only Revision** — `src/environment/revision-capability.ts` (read-only Revision provider)
- **src environment versioning-capability ts** — `src/environment/versioning-capability.ts`
- **src environment versioning-capability ts target operations** — `src/environment/versioning-capability.ts` (target operations)
- **src first-contact extract adapters rule-based-adapter ts** — `src/first-contact/extract/adapters/rule-based-adapter.ts`
- **src first-contact First Contact engine adapters** — `src/first-contact/` — First Contact engine, adapters, and artifact canonicalization.
- **src first-contact project adapters rule-based-adapter ts** — `src/first-contact/project/adapters/rule-based-adapter.ts`
- **src infra filesystem ts FilesystemAdapter interface** — `src/infra/filesystem.ts` — `FilesystemAdapter` interface: `readFile`, `writeFile`, `appendFile`, `mkdir`, `exists`, `readdir`
- **src infra filesystem ts FilesystemAdapter readFile** — `src/infra/filesystem.ts` — `FilesystemAdapter` (readFile, writeFile, appendFile, mkdir, exists, readdir)
- **src infra filesystem ts FilesystemAdapter vs** — `src/infra/filesystem.ts` (`FilesystemAdapter`) vs `src/infra/filesystem-provider.ts` (`FilesystemProvider`)
- **src infra git-adapter ts GitAdapter commit** — `src/infra/git-adapter.ts` — `GitAdapter`: `commit`, `rollback`, `getHeadCommit`, `log` (mutating)
- **src knowledge adapters rule-based-adapter ts** — `src/knowledge/adapters/rule-based-adapter.ts`
- **src knowledge validation adapters rule-based-adapter ts** — `src/knowledge/validation/adapters/rule-based-adapter.ts`
- **src mission-studio adapter-registry ts Mission Studio** — `src/mission-studio/adapter-registry.ts` — Mission Studio planning adapters
- **src repository adapters github-adapter ts GitHubAdapter** — `src/repository/adapters/github-adapter.ts` — `GitHubAdapter implements ForgeAdapter` (repository forge: PRs, releases, checks)
- **src semantic-modeling domain adapters rule-based-adapter ts** — `src/semantic-modeling/domain/adapters/rule-based-adapter.ts`
- **src semantic-modeling intent adapters rule-based-adapter ts** — `src/semantic-modeling/intent/adapters/rule-based-adapter.ts`
- **StateStore** — StateStore
- **StateStore computeHash and InMemoryStateStore computeHash now** — `StateStore.computeHash()` and `InMemoryStateStore.computeHash()` now delegate to `computeStateHash` from `src/runtime/replay.js`.
- **StateStore read-only operational** — StateStore (read-only operational)
- **StateStore state snapshots with hash verification** — StateStore (state snapshots with hash verification)
- **Step 3 Create Capability Registry** — Step 3: Create Capability Registry
- **Step 4 Register the Capability** — Step 4: Register the Capability
- **Step 6 Test** — Step 6: Test
- **Subsystems adapter registry canonical consumer registry** — Subsystems: adapter registry, canonical, consumer registry, correlate, engine, normalize, projection capability executor, replay, session provider, plus `adapters/`, `capabilities/`, `consumers/`, `projections/`, `providers/`.
- **Subsystems with no test coverage** — Subsystems with no test coverage
- **Support multiple versioning strategies through adapters** — Support multiple versioning strategies through adapters.
- **Supported channels latest stable beta nightly** — Supported channels: `latest`, `stable`, `beta`, `nightly`.
- **Survey existing capability definitions in src** — Survey existing capability definitions in `src/capabilities/` and `src/cli/`.
- **Synth Adapter Architecture** — Synth Adapter Architecture
- **synth adapter args delegates to adapter** — `synth adapter <args...>` — delegates to adapter CLI.
- **synth adapter info describes any registered** — `synth adapter info` describes any registered adapter.
- **synth adapter info name creates the** — `synth adapter info <name>` creates the requested adapter (without enabling or configuring it) and emits structured JSON containing `metadata`, `state`, and `health`.
- **Targeted test scripts e g npm** — Targeted test scripts (e.g., `npm run test:certification-framework`, `npm run test:public-vocabulary-audit`)
- **Task runner must be built tested** — Task runner must be built, tested, and certified before the legacy scripts can be removed.
- **TASK-006 npm Adapter** — TASK-006 — npm Adapter
- **TASK-007 CI Orchestration Adapter** — TASK-007 — CI Orchestration Adapter
- **Test Accepted expedition progression** — Test: Accepted expedition progression
- **Test Adjustments** — Test Adjustments
- **Test and script metadata exports** — Test and script metadata exports
- **Test artifact and validation report templates** — Test artifact and validation report templates.
- **Test artifacts** — Test artifacts
- **Test Categories** — Test Categories
- **Test counts per subsystem** — Test counts per subsystem
- **Test coverage** — Test coverage
- **Test coverage Added tests continuous-publication test** — **Test coverage** — Added `tests/continuous-publication.test.js` and wired it into `npm run test:all`.
- **Test coverage changes** — Test coverage changes
- **Test coverage tests core-boundary test js** — **Test coverage:** `tests/core-boundary.test.js` — 6 tests: repo clean, violation detection (plain + `node:`-prefixed), crypto allowed, non-Core directories ignored, missing directories tolerated.
- **Test coverage tests environment-capability-report test js** — **Test coverage:** `tests/environment-capability-report.test.js` — 8 tests covering 12-family completeness, supported/unsupported entries, unavailable list, assumptions mapping, embedded guidance, determinism, and markdown rendering.
- **Test coverage tests environment-discovery-evidence test js** — **Test coverage:** `tests/environment-discovery-evidence.test.js` — 9 tests covering canonicalization determinism, timestamp-insensitive hashing, tamper detection (hash + replay divergence), derived-section reproduction, persistence round-trip, and invalid-artifact handling.
- **Test coverage tests environment-forge-capability test js** — **Test coverage:** `tests/environment-forge-capability.test.js` — 8 tests covering repository metadata parsing, failure tolerance, issue listing with labels, malformed-entry skipping, PR branch mapping, release listing, unparseable output, and `gh` failure handling — all via scripted ToolProvider fakes (no live `gh` calls).
- **Test coverage tests environment-process-capability test js** — **Test coverage:** `tests/environment-process-capability.test.js` — 9 tests covering stdout/stderr capture, non-zero exit codes, missing commands as data, stdin piping, timeouts, `cwd` handling, tool location/availability, and tool execution.
- **Test coverage tests environment-secrets-capability test js** — **Test coverage:** `tests/environment-secrets-capability.test.js` — 10 tests covering secret retrieval, missing secrets, existence checks, name-only listing (with explicit no-value-leak assertions), identity resolution and fallback precedence, CI detection, and empty environments.
- **Test coverage tests npm test npm** — Test coverage (`tests/`, `npm test`, `npm run test:*`)
- **Test evidence** — Test evidence
- **Test examples that verify actual behavior** — Test examples that verify actual behavior
- **Test Execution without expedition** — Test: Execution without expedition
- **Test for the homepage** — Test for the homepage
- **test how it observes without mutation** — `test` — how it observes without mutation.
- **Test infrastructure is unified and shared** — Test infrastructure is unified and shared.
- **Test Invariants** — Test Invariants
- **Test Plan creation outside lifecycle** — Test: Plan creation outside lifecycle
- **test protected-asset-escalation added to package json** — `test:protected-asset-escalation` added to `package.json` and `test:all`; capability map extended with `ProtectedAssets` capability.
- **Test provenance npm test** — **Test provenance:** `npm test`
- **Test Results** — Test Results
- **Test Scenario 1 Repository Introduction** — Test Scenario 1: Repository Introduction
- **Test Scenario 2 Create New Capability** — Test Scenario 2: Create New Capability
- **Test Scenario 3 Ambiguous Request** — Test Scenario 3: Ambiguous Request
- **Test Scenario 4 Recovering From Wrong** — Test Scenario 4: Recovering From Wrong Model
- **Test scenarios** — 1.2 Test scenarios
- **Test Suite Reverse Engineering Classification Collision** — Test Suite Reverse Engineering — Classification & Collision Analysis
- **Test Suites 154 passing** — Test Suites (154 passing)
- **test test-related finding** — `test` — test-related finding
- **Test Valid completion** — Test: Valid completion
- **test-only hooks** — test-only hooks.
- **Test-only hooks or environment variables not** — Test-only hooks or environment variables not documented as public.
- **Testability** — Testability
- **Testing** — Testing
- **Testing approaches** — Testing approaches
- **Testing architectural constraints requires integration-level tests** — Testing architectural constraints requires integration-level tests
- **Testing Determinism** — Testing Determinism
- **Testing every possible repository shape this** — Testing every possible repository shape (this is the canonical regression, not an exhaustive matrix).
- **Testing human operators this is an** — Testing human operators; this is an automated benchmark of repository authority.
- **Testing Impact** — 9. Testing Impact
- **Testing is reproducible** — Testing is reproducible
- **Testing Methodology** — Testing Methodology
- **Testing Philosophy** — Testing Philosophy
- **Testing Replay** — Testing Replay
- **Testing requires exercising the full pipeline** — Testing requires exercising the full pipeline, not just individual functions
- **Testing Standards** — Testing Standards
- **Testing validation and developer experience** — Testing, validation, and developer experience.
- **tests** — `tests`
- **tests adapter-bdd test js 8 tests** — `tests/adapter-bdd.test.js` — 8 tests covering registry listing, lifecycle, feature/scenario creation, acceptance-test generation, evidence generation, and health checks.
- **Tests added in tests impact-analyzer test** — Tests added in `tests/impact-analyzer.test.js` and wired into `npm run test:all`.
- **Tests added in tests validation-mapping test** — Tests added in `tests/validation-mapping.test.js` covering valid map, script references, Protected Asset rules, and failure modes.
- **Tests added in tests validation-planner test** — Tests added in `tests/validation-planner.test.js` and wired into `test:all` via `test:validation-planner`.
- **tests adoption-validation test js verifies protocols** — `tests/adoption-validation.test.js` — verifies protocols, benchmark, report, and npm scripts.
- **tests ai-benchmark test js harness verification** — `tests/ai-benchmark.test.js` — harness verification tests.
- **tests ai-benchmark-fixtures prompt-suite json bootstrap mission-create** — `tests/ai-benchmark-fixtures/prompt-suite.json` — bootstrap, mission-create, explain-replay.
- **tests ai-benchmark-fixtures repository-suite json synthetic repositories** — `tests/ai-benchmark-fixtures/repository-suite.json` — synthetic repositories: empty, node, polyglot.
- **Tests are deterministic and repeatable** — Tests are deterministic and repeatable
- **Tests are reliable across runs** — Tests are reliable across runs
- **tests brownfield-validation test js validates bootstrap** — `tests/brownfield-validation.test.js` — validates bootstrap analysis on six certified examples.
- **tests cli-contract test js** — `tests/cli-contract.test.js`
- **tests continuous-publication test js** — `tests/continuous-publication.test.js`.
- **tests convergence-certification test js** — `tests/convergence-certification.test.js`
- **Tests cover all five supported architectural** — Tests cover all five supported architectural styles and lifecycle.
- **Tests cover all five supported specification** — Tests cover all five supported specification formats plus lifecycle.
- **Tests cover all three graph types** — Tests cover all three graph types and lifecycle.
- **Tests cover directory scanning explicit file** — Tests cover directory scanning, explicit file list, markdown/text extraction, binary placeholder, missing directory, and lifecycle.
- **Tests cover expedition preservation mission conversion** — Tests cover expedition preservation, mission conversion, multi-theme splitting, and lifecycle.
- **Tests cover initialization revision creation branch** — Tests cover: initialization, revision creation, branch switching, comparison, snapshot, merge integration, determinism, and non-repository rejection.
- **Tests cover lifecycle intent extraction actor** — Tests cover lifecycle, intent extraction, actor extraction, constraint extraction, and unknown fallback.
- **Tests cover macOS and Linux detection** — Tests cover macOS and Linux detection paths.
- **Tests cover mission capability component constraint** — Tests cover mission, capability, component, constraint, and risk extraction.
- **Tests cover mission preservation intent conversion** — Tests cover mission preservation, intent conversion, capability fallback, and lifecycle.
- **Tests cover objective preservation expedition conversion** — Tests cover objective preservation, expedition conversion, and lifecycle.
- **Tests cover score computation missing evidence** — Tests cover score computation, missing evidence, ambiguity, and conflict detection.
- **Tests do not need to change** — Tests do not need to change.
- **tests documentation-expedition test js** — `tests/documentation-expedition.test.js`
- **tests documentation-expedition test js 12 tests** — `tests/documentation-expedition.test.js` — 12 tests covering extraction, graph building, normalization, projection, and determinism.
- **tests documentation-integrity test js** — `tests/documentation-integrity.test.js`.
- **tests documentation-integrity test js Covers link** — **`tests/documentation-integrity.test.js`** — Covers link checking, projection verification script presence, version sync script presence, and npm script registration.
- **tests documentation-integrity test js extended new** — `tests/documentation-integrity.test.js` extended: new negative test proves the canonical-URL check fails on a stale URL; the script-exists test asserts the check is configured. All 8 documentation integrity tests pass.
- **tests environment-discovery test js regression tests** — `tests/environment-discovery.test.js` — regression tests for repository, branch, PR, and non-repository cases.
- **tests environment-forge-capability test js regression tests** — `tests/environment-forge-capability.test.js` — regression tests for new operations.
- **tests environment-versioning-capability test js regression tests** — `tests/environment-versioning-capability.test.js` — regression tests for all operations.
- **tests environment-versioning-certification test js certification suite** — `tests/environment-versioning-certification.test.js` — certification suite exercising real Git operations through `GitVersioningProvider`.
- **tests execution-runtime test js regression tests** — `tests/execution-runtime.test.js` — regression tests for synthesis, dispatch, ordering, success, and failure paths.
- **Tests existed first** — Tests existed first
- **tests expedition-lifecycle test js Now creates** — `tests/expedition-lifecycle.test.js` — Now creates and approves a Mission before creating Expeditions; added explicit missing-mission rejection test.
- **Tests failed first** — Tests failed first
- **tests freeze-certification test js automated verification** — `tests/freeze-certification.test.js` — automated verification that all freeze artifacts exist and the latest proof passed.
- **tests govern-profiler test js tests governance-incremental** — `tests/govern-profiler.test.js`, `tests/governance-incremental.test.js`, `tests/governance-optimization.test.js`.
- **tests governance-lifecycle-contract test js** — `tests/governance-lifecycle-contract.test.js`
- **tests governance-lifecycle-contract test js Uses the** — `tests/governance-lifecycle-contract.test.js` — Uses the runtime `missionId` returned by `synth mission approve`.
- **tests governance-orchestration test js benchmark tests** — `tests/governance-orchestration.test.js` — benchmark tests.
- **tests governance-orchestration test js cache lifecycle** — `tests/governance-orchestration.test.js` — cache lifecycle tests.
- **tests governance-orchestration test js CI plan** — `tests/governance-orchestration.test.js` — CI plan tests.
- **tests governance-orchestration test js end-to-end planning** — `tests/governance-orchestration.test.js` — end-to-end planning tests.
- **tests governance-orchestration test js execution tests** — `tests/governance-orchestration.test.js` — execution tests.
- **tests governance-orchestration test js profile tests** — `tests/governance-orchestration.test.js` — profile tests.
- **tests governance-orchestration test js verifies graph-driven** — `tests/governance-orchestration.test.js` — verifies graph-driven scheduling.
- **tests impact-analyzer test js verifies classification** — `tests/impact-analyzer.test.js` — verifies classification behavior.
- **tests installer-contract test js verifies argument** — `tests/installer-contract.test.js` verifies argument parsing, exit codes, help output, and environment variable handling.
- **tests installer-distribution test js verifies default** — `tests/installer-distribution.test.js` verifies default/latest resolution, exact version, stable channel, and unknown channel failure.
- **tests installer-environment test js verifies the** — `tests/installer-environment.test.js` verifies the profile is printed and the current platform is detected as supported.
- **tests installer-manifest test js covers generation** — `tests/installer-manifest.test.js` covers generation, validation, and installer consumption.
- **tests operator-journey test js automated synthetic** — `tests/operator-journey.test.js` — automated synthetic operator that executes the full journey and verifies each artifact.
- **tests operator-journey test js automates a** — `tests/operator-journey.test.js` automates a synthetic operator through the full journey.
- **Tests pass in CI** — Tests pass in CI.
- **Tests passed 119 119** — Tests passed: 119/119
- **Tests prove blocked allowed scope behavior** — Tests prove blocked/allowed/scope behavior.
- **Tests prove that equivalent intent across** — Tests prove that equivalent intent across different adapter shapes converges to the same governed project model.
- **Tests prove that initialization cannot introduce** — Tests prove that initialization cannot introduce implementation assumptions or create expeditions/missions.
- **tests public-vocabulary-audit test js** — `tests/public-vocabulary-audit.test.js`
- **tests public-vocabulary-audit test js automated audit** — `tests/public-vocabulary-audit.test.js` — automated audit that fails if implementation terms leak into public docs.
- **tests public-vocabulary-audit test js locks public** — `tests/public-vocabulary-audit.test.js` locks public docs against unreviewed terminology additions.
- **tests public-vocabulary-audit test js scans operator** — `tests/public-vocabulary-audit.test.js` scans operator docs, introduction, and reference docs for forbidden implementation terms.
- **tests README md** — `tests/README.md`
- **Tests reference missing functionality** — Tests reference missing functionality.
- **tests repository-governance test js exercises classifyBranch** — `tests/repository-governance.test.js` exercises `classifyBranch`, `validateBranchName`, and `generateBranchName`.
- **tests repository-governance test js validates inferVersionBump** — `tests/repository-governance.test.js` validates `inferVersionBump` and `nextSemanticVersion`.
- **tests repository-governance test js validates PR** — `tests/repository-governance.test.js` validates PR replay semantics.
- **tests repository-governance test js validates promotion** — `tests/repository-governance.test.js` validates promotion rules.
- **tests repository-governance test js validates release** — `tests/repository-governance.test.js` validates release replay and release notes.
- **tests repository-identity test js covers the** — `tests/repository-identity.test.js` covers the root repository, a fresh project after `synth init`, and an empty directory.
- **tests runtime-integrity test js verifies both** — `tests/runtime-integrity.test.js` verifies both a passing integrity check and detection of a modified dist file.
- **tests succeed** — tests succeed
- **tests synth test js** — `tests/synth.test.js`
- **tests synth test js 1397-1403 language** — `tests/synth.test.js:1397-1403` — language audit on `dist/main.js` passes because `main.js` does not contain the three narrowly checked Ticket patterns.
- **tests synth test js 511-537 Layer** — `tests/synth.test.js:511-537` — Layer 4 replay tests use a hand-rolled `rebuildState` and structural checks, not `ReplayVerifier`.
- **tests synth test js 943-953 Expedition** — `tests/synth.test.js:943-953` — "Expedition: replay reconstructs planning state correctly" asserts `replayVerifier.verify().consistent === true`, which is hard-coded.
- **tests synth test js imports dist** — `tests/synth.test.js` imports `dist/synth-v5.js`.
- **tests synth test js Updated registry** — `tests/synth.test.js` — Updated registry size assertion, fixed `StartWorkItem` seal test to create a fresh work item, and changed `AcceptDecision` tests to use `RecordDecision` with a full payload.
- **tests synth-bootstrap test js covers empty** — `tests/synth-bootstrap.test.js` — covers empty, existing, brownfield, and polyglot cases.
- **tests synth-cli test js CLI test** — `tests/synth-cli.test.js` — CLI test coverage.
- **tests synth-cli-govern-explain test js CLI explanation** — `tests/synth-cli-govern-explain.test.js` — CLI explanation tests.
- **tests synth-cli-repo test js exercises synth** — `tests/synth-cli-repo.test.js` exercises `synth repo branch create`.
- **tests synth-cli-repo test js verifies synth** — `tests/synth-cli-repo.test.js` verifies `synth repo init` rejects unknown forge providers and accepts GitHub.
- **Tests tests ai-benchmark test js added** — **Tests:** `tests/ai-benchmark.test.js` added and wired into `npm run test:all` via `test:ai-benchmark`.
- **Tests tests resume-briefing test js covers** — **Tests** — `tests/resume-briefing.test.js` covers uninitialized, initialized, approved, executing, determinism, and broken-decision-log scenarios; wired into `test:all` as `test:resume-briefing`.
- **Tests tests synth-bootstrap test js covers** — **Tests**: `tests/synth-bootstrap.test.js` covers all four repository types, dry-run non-mutation, and approved application.
- **Tests that asserted derived fields directly** — Tests that asserted derived fields directly must call builder functions.
- **Tests that pass on one run** — Tests that pass on one run and fail on the next
- **tests validation-planner test js updated with** — `tests/validation-planner.test.js` — updated with class/profile test cases.
- **Tests verify language dependency and tooling** — Tests verify language, dependency, and tooling observations.
- **The adapter can be enabled disabled** — The adapter can be enabled/disabled without affecting Kernel behavior.
- **The adapter environment discovery consolidation removed** — The adapter/environment/discovery consolidation removed the largest source of structural duplication.
- **The Adapter Health Model unknown healthy** — The Adapter Health Model (`unknown | healthy | degraded | unhealthy | disabled`) is frozen.
- **The adapter is registered in AdapterRegistry** — The adapter is registered in `AdapterRegistry`.
- **The Adapter Lifecycle discover configure validate** — The Adapter Lifecycle (`discover → configure → validate → enable → healthy → operational → disable`) is frozen.
- **The Adapter Registry interface is frozen** — The Adapter Registry interface is frozen.
- **The adapter serves as the canonical** — The adapter serves as the canonical reference implementation for all future Synth adapters.
- **The adapter skips node modules git** — The adapter skips `node_modules`, `.git`, `dist`, and other build artifacts.
- **The adapter transforms external project context** — The adapter transforms external project context into a governed SYNTH project model.
- **the adapter version** — the adapter version,
- **The build test audit and governance** — The build, test, audit, and governance pipelines pass.
- **the capability family Versioning** — the capability family (`Versioning`)
- **The Capability Graph** — The Capability Graph
- **The capability itself operates on feature** — The capability itself operates on feature lists; extraction is a separate adapter concern.
- **The capability registry is frozen** — The capability registry is frozen.
- **The capability registry is frozen No** — The capability registry is frozen. No new capabilities can be registered.
- **The Capability Report ADR-016** — The Capability Report ([ADR-016](../adr/ADR-016-ai-environment-planning.md))
- **The capability verification report is not** — The capability verification report is not `passed`.
- **The CapabilityRegistry becomes immutable no new** — The CapabilityRegistry becomes immutable (no new capabilities)
- **The ExecutionArtifactAdapter is the only place** — The ExecutionArtifactAdapter is the only place where external artifact formats exist
- **The filesystem adapter conforms to the** — The filesystem adapter conforms to the `InitializationAdapter` contract.
- **The following agentic smoke test completes** — The following agentic smoke test completes successfully:
- **The InitializationAdapter and ProjectModel contracts are** — The `InitializationAdapter` and `ProjectModel` contracts are stable and type-checked.
- **The intent s capability is in** — The intent's capability is in the policy's scope
- **The Kernel depends only on adapter** — The Kernel depends only on adapter interfaces.
- **The kernel test** — The kernel test
- **The ObservableAdapter interface extends the canonical** — The `ObservableAdapter` interface extends the canonical `Adapter` interface.
- **The operator adapter is injectable** — The operator adapter is injectable.
- **The package json test all script** — The package.json `test:all` script itself
- **The product-boundary GitHubAdapter in src adapters** — The product-boundary `GitHubAdapter` in `src/adapters/github/` is unchanged; the Forge capability is an Environment Layer artifact. Core migration is deferred to EXP-ENV-012 per program sequencing.
- **The same artifact and adapter version** — The same artifact and adapter version produce the same model.
- **The same capability can have different** — The same capability can have different policies in different deployments
- **The same domain model and adapter** — The same domain model and adapter version produce the same knowledge graph.
- **The same intent model and adapter** — The same intent model and adapter version produce the same domain model.
- **Their state is verifiable hashes attestations** — Their state is verifiable (hashes, attestations, chain integrity)
- **Third-party adapter ecosystem** — Third-party adapter ecosystem
- **Threshold is configurable and testable across** — Threshold is configurable and testable across breakpoints.
- **Tool Capability Interface** — 2. Tool Capability Interface
- **Translating adapter Observation into PlanningObservation via** — Translating adapter `Observation[]` into `PlanningObservation[]` via the Mission Intake mapper
- **Trust Boundaries Capability Isolation Compromise scope** — **Trust Boundaries** + **Capability Isolation** = Compromise scope is limited
- **Two agents using the same adapter** — Two agents using the same adapter version converge on substantially equivalent artifacts.
- **Two Capability Layers** — Two Capability Layers
- **Two extension models src adapters and** — **Two extension models:** `src/adapters/` and `src/environment/providers/` both integrate external systems.
- **Typical use documentation quality test quality** — Typical use: documentation quality, test quality, generated assets, naming consistency, style consistency, checklist-based code review, low-stakes generated artifacts.
- **Uniform adapter types file pair for** — **Uniform adapter/types file pair for every external interface**, even when the interface is trivial.
- **Unit tests** — 1.2 Unit tests
- **Unit tests and integration tests remain** — Unit tests and integration tests remain separate concerns. Certifications validate operator-facing behavior, not implementation internals.
- **Unit tests pass for design system** — Unit tests pass for design system, components, and Mission Studio UI.
- **Unknown adapters exit non-zero with a** — Unknown adapters exit non-zero with a structured error.
- **Unknown adapters fail with a clear** — Unknown adapters fail with a clear error.
- **Update capability-validation-map json** — Update capability-validation-map.json.
- **Update contract tests to assert the** — Update contract tests to assert the expanded surface.
- **Update docs reference capability-validation-map json with** — Update `docs/reference/capability-validation-map.json` with a PackageDistribution capability.
- **Update path constants in tests that** — Update path constants in tests that assert on default layout.
- **Update tests and fixtures** — 4. Update tests and fixtures
- **Update tests to assert the expanded** — Update tests to assert the expanded surface.
- **Updated docs architecture 07-capability-model md with** — Updated `docs/architecture/07-capability-model.md` with a **Two Capability Layers** section, a **Capability Graph** section (node kinds, edge kinds, deterministic resolution), and ADR cross-links.
- **Updated tests documentation-integrity test js for** — Updated `tests/documentation-integrity.test.js` for the new validation semantics.
- **Updated tests documentation-integrity test js to** — Updated `tests/documentation-integrity.test.js` to assert the new script exists, is configured, and passes on the repository.
- **Updated tests synth-cli test js to** — Updated `tests/synth-cli.test.js` to assert `ValidationPlan` output and the new flags.
- **Upgrade path test no breaking changes** — Upgrade path test: no breaking changes incompatible with the rc.3 baseline.
- **Use a private capability token passed** — Use a private capability token passed only by the gate, or encapsulate the store so `append` is not reachable outside the gate module.
- **Use ForgeCapability to create pull requests** — Use ForgeCapability to create pull requests.
- **Use the Forge capability for API** — Use the Forge capability for API interactions.
- **use the VersioningCapability for all repository** — use the VersioningCapability for all repository history operations
- **Valid Register capability during bootstrap before** — Valid: Register capability during bootstrap (before seal)
- **Validate the prompt suite tests ai-benchmark-fixtures** — Validate the prompt suite (`tests/ai-benchmark-fixtures/prompt-suite.json`).
- **Validate the repository suite tests ai-benchmark-fixtures** — Validate the repository suite (`tests/ai-benchmark-fixtures/repository-suite.json`).
- **validateCapability resolveIntent** — `validateCapability()` → `resolveIntent()`
- **Validation Tests 4 tests** — Validation Tests (4 tests)
- **Validator implemented in scripts verify-capability-validation-map js** — Validator implemented in `scripts/verify-capability-validation-map.js`:
- **Value core guarantee protected user-facing capability** — **Value:** core guarantee protected, user-facing capability, production workflow exercised.
- **Value delivered user-facing capability or core** — **Value delivered** — user-facing capability or core guarantee protected.
- **verificationReport a CapabilityVerificationReport with status passed** — `verificationReport`: a `CapabilityVerificationReport` with status `passed`.
- **Verified tests pass with node tests** — Verified tests pass with `node tests/environment-discovery.test.js`.
- **Verified TypeScript compilation and test suite** — Verified TypeScript compilation and test suite.
- **Verified TypeScript compilation and unit tests** — Verified TypeScript compilation and unit tests.
- **Verify integration with npm run test** — Verify integration with `npm run test:all`.
- **Version control existed as a capability** — Version control existed as a capability, but no Expedition could invoke it.
- **Versioning Capability** — Versioning Capability
- **VersioningCapability interface** — 1. `VersioningCapability` interface
- **VersioningCapability interface is defined and documented** — `VersioningCapability` interface is defined and documented.
- **Visual regression tests** — 1.5 Visual regression tests
- **Visual regression tests pass for homepage** — Visual regression tests pass for homepage, phases, sidebar, artifacts, components, breakpoints, and themes.
- **Website pages accurately reference the capability** — Website pages accurately reference the capability reference, Environment Layer reference, and updated guides.
- **What enters Actor capability payload all** — **What enters:** Actor, capability, payload (all validated)
- **What Is a Capability** — What Is a Capability?
- **What tests prove the contract is** — What tests prove the contract is satisfied.
- **What the system can do Capability** — What the system can do (Capability Registry)
- **What to Test** — What to Test
- **What was done capability** — What was done (capability)
- **When to Add a Capability** — When to Add a Capability
- **whether tests exist** — whether tests exist
- **Which tests fail if the subsystem** — Which tests fail if the subsystem is removed?
- **Why Cryptographic Attestation** — Why Cryptographic Attestation
- **Wired into governance via test validation-mapping** — Wired into governance via `test:validation-mapping` and included in `npm run test:all`.
- **Wired test adapter-introspection into test all** — Wired `test:adapter-introspection` into `test:all`.
- **Wired test clean-machine-output into test all** — Wired `test:clean-machine-output` into `test:all`.
- **Wired test operator-briefing into test all** — Wired `test:operator-briefing` into `test:all`.
- **Wiring into test all in package** — Wiring into `test:all` in `package.json`.
- **Work for all adapters registered in** — Work for all adapters registered in `AdapterRegistry`.
- **Workspace Capability Interface** — 1. Workspace Capability Interface
- **Workspace WorkspaceCognitionEnvironment CanonicalLanguageAuditor RepositoryHealth SemanticVerifier ExecutionArtifactAdapter** — Workspace: `WorkspaceCognitionEnvironment`, `CanonicalLanguageAuditor`, `RepositoryHealth`, `SemanticVerifier`, `ExecutionArtifactAdapter`.
- **Writing New Tests** — Writing New Tests

## Getting Started

- Run `npm run govern` to verify the full pipeline.
- Add new capabilities through the Capability Registry.
- Build adapters that conform to the Adapter Architecture Specification.
- Write tests before implementation.

<!--
sourceStateHash: c81f49c94cd51c4057ae2bf878e1603001e132101bb69661a8197e18011e4b0b
projection: synth-documentation-expedition-v1
-->