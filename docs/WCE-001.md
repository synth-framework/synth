# WCE-001: Workspace Cognition Environment

## Status: Implemented
## Date: 2026-06-28
## Classification: Architectural Component

---

## Purpose

WCE-001 is the **deterministic orientation layer** for every engineering session.

> The first job of an autonomous agent is to understand where it is.

Instead of agents blindly exploring (`pwd`, `ls`, `find`, `cat README`), the workspace answers those questions deterministically before the agent asks them.

This is not CLI polish. This is the **canonical entry point into the engineering operating system**.

---

## The Deterministic Orientation Pipeline

Every session follows the same fixed sequence:

```
Workspace Boot
    ↓
Identity
    ↓
Environment
    ↓
Architecture
    ↓
Repository Health
    ↓
Current Engineering Context
    ↓
Suggested Actions
```

Exactly the same every session. Every agent gets the same mental model.

---

## Architecture

### Components

| Component | Responsibility |
|-----------|---------------|
| `WorkspaceCognitionEnvironment` | Orchestrates the orientation pipeline |
| `CanonicalLanguageAuditor` | Verifies architectural language compliance |
| `RepositoryHealth` | Checks repository, runtime, architecture health |

### Machine Interface

```
.synth/
    workspace.json     ← Full workspace descriptor
    health.json        ← Repository health report
    context.json       ← Current engineering context
```

The banner is a rendering. The real interface is structured data. Agents consume `workspace.json` directly. The CLI renders it. VS Code renders it. A web dashboard renders it. One source of truth.

---

## Phase 1: Identity

```
System: Synth v2
Version: 2.0.0
Description: Deterministic Execution Kernel + Planning Cognition Engine
Layers: Authority, Mutation, Execution, Truth, Determinism
```

Answers: "What system is this?"

---

## Phase 2: Environment Verification

```
Environment

✓ Node.js v20.20.2
✓ Event Store available
✓ Replay Verifier available
✓ 21 execution primitives registered
✓ Workspace writable
```

Answers: "What can this system do?"

Status: `READY`, `DEGRADED`, or `BLOCKED`.

---

## Phase 3: Architecture Verification

```
Canonical Language

Planning
✓ Mission
✓ Expedition
✓ Objective
✓ Discovery
✓ Decision

Execution
✓ WorkItem

Governance
✓ Policy
✓ Invariant
✓ Seal

Infrastructure
✓ Event
✓ Replay
✓ Determinism
```

The workspace itself enforces architecture. It runs a Canonical Language Audit on boot.

---

## Phase 4: Repository Health

```
Repository Health

Architecture
✓ Constitution present
✓ Knowledge Base present
✓ ADRs valid

Runtime
✓ Event log valid
✓ Replay deterministic
✓ Policies loaded

Quality
✓ Tests passing
✓ No forbidden imports
✓ No kernel drift

Documentation
✓ Cross references valid
✓ Knowledge graph complete
```

Not "npm test." A comprehensive health model.

---

## Phase 5: Engineering Context

```
Engineering Context

Mission: Execution Kernel v3

Expedition: Artifact Independence

Objectives
✓ Remove Ticket domain
✓ Remove aliases
▶ Build Agent Workspace

Pending Decisions: 2
Pending Discoveries: 1
Work Items: 5
Confidence: 93%
```

This is cognition, not source control. The workspace displays the current expedition, not the current Git branch.

---

## Phase 6: Suggested Actions

```
Suggested Actions

▶ Continue Expedition
○ Review Architecture
○ Inspect Discoveries
○ Run Health Audit
```

Engineering activities, not shell commands. These map to commands internally, but the agent sees intent.

---

## Machine-Readable Interface

### workspace.json

```json
{
  "version": "1.0.0",
  "generatedAt": "2026-06-28T00:00:00.000Z",
  "identity": {
    "system": "Synth v2",
    "version": "2.0.0",
    "layers": ["Authority", "Mutation", "Execution", "Truth", "Determinism"]
  },
  "environment": {
    "runtime": { "name": "Node.js", "version": "v20.20.2", "ok": true }
  },
  "architecture": {
    "ubiquitousLanguage": { /* all canonical terms */ },
    "invariants": [
      { "id": "I1", "text": "Planner never emits execution instructions directly" }
    ]
  },
  "health": { /* full health report */ },
  "engineeringContext": {
    "missionCount": 2,
    "expeditionCount": 2,
    "objectiveCount": 1,
    "discoveryCount": 1,
    "decisionCount": 0,
    "workItemCount": 5,
    "eventCount": 42
  },
  "suggestedActions": [
    { "action": "Continue Expedition", "priority": "high" }
  ]
}
```

### health.json

```json
{
  "status": "ready",
  "checks": [
    { "category": "runtime", "name": "Event log valid", "status": "pass" }
  ],
  "summary": { "pass": 5, "check": 6, "fail": 0 }
}
```

### context.json

```json
{
  "missions": [...],
  "expeditions": [...],
  "objectives": [...],
  "discoveries": [...],
  "decisions": [...],
  "workItems": [...],
  "eventCount": 42
}
```

---

## Ubiquitous Language

WCE-001 enforces the Ubiquitous Language defined in `ubiquitous-language.md`.

### Governance Rules

1. **Planning documents must use Planning Layer terms only.**
2. **Execution documents must use Execution Layer terms for execution.**
3. **Architecture documents must not use Projection Layer terms.**
4. **Forbidden terms appear only in migration reports and historical ADRs.**

### Audit Questions

| Question | Checks For |
|----------|-----------|
| Does any infrastructure term appear in the planning layer? | Leakage of DB, network, file system concepts |
| Does any persistence term appear in the cognition layer? | Leakage of storage, caching, serialization |
| Does any vendor-specific concept appear in the canonical model? | Leakage of GitHub, Jira, AWS |
| Does any implementation optimization appear in architectural docs? | Leakage of performance, caching, batching |
| Does any execution primitive appear in planning contracts? | Leakage of capability names into intent |

---

## Relationship to Other Documents

```
Architectural Constitution
        ↓
Ubiquitous Language  ← vocabulary contract
        ↓
Agent Constitution  ← behavioral rules
        ↓
Knowledge Base  ← all documentation
        ↓
WCE-001  ← orientation + enforcement
        ↓
Implementation  ← code
```

---

## Acceptance Criteria

- [x] Deterministic orientation pipeline (6 phases)
- [x] Canonical Language Audit on boot
- [x] Repository Health checks
- [x] Human-readable banner
- [x] Machine-readable descriptors (workspace.json, health.json, context.json)
- [x] Engineering context display (not source control)
- [x] Suggested Actions (engineering activities, not commands)
- [x] All tests pass
- [x] No replay regression

---

## Files

| File | Purpose |
|------|---------|
| `dist/synth-v5.js` | WCE classes + orientation pipeline |
| `docs/WCE-001.md` | This specification |
| `docs/ubiquitous-language.md` | Vocabulary contract |
| `docs/INTENT-001.md` | Architectural vision |
| `.synth/workspace.json` | Machine-readable descriptor |
| `.synth/health.json` | Health report |
| `.synth/context.json` | Engineering context |

---

*WCE-001: The deterministic orientation layer for every engineering session.*
*Status: Implemented. 106 tests passing.*
