# Workspace Rules — synth-v2

## Synth Binary

Always use `node dist/cli/synth.js` as the synth binary in the synth-v2 workspace. Never use `dist/main.js`, `npx @synth-framework/synth`, or any other path.
