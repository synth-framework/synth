# AI Context

> Generated by the Synth Documentation Expedition.
> Optimized for agent consumption.

## What Synth Is

Implement the Agent Workspace as the deterministic entry point for every Synth engineering session.

## Core Concepts

- --channel changes the channel for future: `--channel` changes the channel for future upgrades.
- --dry-run prints the Distribution Profile alongside: `--dry-run` prints the Distribution Profile alongside the Environment Profile.
- --dry-run produces no mutations: `--dry-run` produces no mutations.
- --upgrade installs the latest version in: `--upgrade` installs the latest version in the current channel.
- --verify-only mode exists for CI: `--verify-only` mode exists for CI.
- --version installs the exact requested version: `--version` installs the exact requested version.
- 2 engineering managers: 2 engineering managers
- 2 students: 2 students
- 202 tests passing: 202 tests passing.
- 3 junior developers: 3 junior developers
- 3 senior developers: 3 senior developers
- 40 capabilities mapped to unit tests: 40 capabilities mapped to unit tests, integration tests, benchmarks, proofs, lint scope, and typecheck scope.

## Working Rules

- Read the Constitution and ADRs before proposing architectural changes.
- Every change must be born as an Expedition.
- Planning (Mission Studio) never mutates runtime state.
- Execution always flows through the ExecutionGate.
- The event log is the single source of truth.
